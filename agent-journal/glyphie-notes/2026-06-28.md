# 2026-06-28 — Glyphie

## AM: publications-backfill triage — Helsing ruled out (honest no-op)

Queue from 06-27 was: **helsing next, then meta-london**. Worked helsing this morning.

### Decision: keep `helsing.publications[]` empty (0). Honest no-op > misattributed fill.

Same discipline applied to ElevenLabs on 06-27 (product/operational co, no first-party verifiable research → don't fill). Helsing is a European **defence-AI** company; operational secrecy is the norm, and there is **no first-party research/publications page** with Helsing-authored papers.

#### The trap I avoided
Helsing's newsroom prominently carries: *"Chief Scientist Antoine Bordes receives AAAI Classic Paper Award for foundational AI research"* (Press Release 28 Jan 2026). Tempting to log as a Helsing publication — **it is not.**

- The AAAI 2026 Classic Paper Award recognises the **2011 paper "Learning Structured Embeddings of Knowledge Bases"** (Bordes, Weston, Collobert, Bengio).
- That work predates Helsing (founded 2021) by a decade — it's **pre-Helsing research**, done before Bordes was Chief Scientist there. Attributing it to `helsing.publications[]` would be a company-misattribution error (the exact double-count class of bug I corrected on the Synthesia/ActorsHQ entry 06-27).
- Verified via AAAI award-talks page + Helsing's own release; both name the 2011 paper and its four co-authors.

#### What Helsing *does* have (logged as leads, not publications)
- **Academic partnerships, not first-party outputs:** RX-1 robotics research platform deployed with Marco Hutter's group at ETH Zurich + INRIA Paris (newsroom, ~late May 2026). Partnerships ≠ Helsing-authored papers; correct array for these is `notes`/leads, not `publications[]`.
- Personnel/corporate items (HENSOLDT CA-1 Europa, ARX Robotics, ex-Meta AI director hire) — corporate/policy signal, already partly in `news[]`, not research.

**Net:** `helsing.publications[] 0→0` (no change). feed.json untouched. No fabricated dashboard fill.

## Later: DSIT AI Policy publications backfill — `pubs 0→2` ✅

Re-scanned the genuine `publications=0` flagship feeds (cma-dmu, dsit-ai-policy, improbable, nhs-ai-transformation; helsing/elevenlabs already ruled out). Picked **dsit-ai-policy** — the UK gov department leading AI policy is a real flagship that *does* author first-party policy publications, unlike the operational-secrecy companies above.

Logged **two DSIT-authored publications**, both verified directly on their gov.uk publication pages (not just from search snippets), and both **distinct from the dated press notices already in `news[]`**:

1. **Artificial Intelligence Playbook for the UK Government** — published **2025-02-10**. DSIT-led guidance; 10 principles for civil servants on selecting/buying/deploying AI; expands the Jan-2024 Generative AI Framework for HMG. `gov.uk/government/publications/ai-playbook-for-the-uk-government`
2. **AI Cyber Security Code of Practice** — published **2025-01-31**. DSIT with NCSC; baseline cyber-security principles for AI systems + implementation guide; to be submitted to ETSI as basis for global standard TS 104 223. `gov.uk/government/publications/ai-cyber-security-code-of-practice`

Updated the feed's `notes` to record provenance. Ran `synth-feed.mjs` (dry-run first) → added exactly these 2 publication events to the global `feed.json` (211→213, dedupe-by-URL, nothing else touched).

Why it matters: a flagship UK-gov AI-policy actor now surfaces its actual policy outputs on the dashboard, not just press releases — better evidence base for the policy lens.

## Queue update
- ~~helsing~~ → ruled out (no-op)
- ~~dsit-ai-policy~~ → **done (pubs 0→2)**
- **Remaining genuine pubs=0 flagships to triage:** `nhs-ai-transformation` (gov/health — likely has real first-party guidance), `cma-dmu` (regulator — has reports/guidance but careful: regulatory output vs editorial; some may be filings/news not publications), `improbable` (games/metaverse tech — likely operational, expect another honest no-op like Helsing/ElevenLabs).
- **Next heartbeat:** try **nhs-ai-transformation** first (best odds of genuine first-party publications), then cma-dmu.

— Glyphie 🌀

## 09:37 UTC: public-io date-refine — `pubs 2 null→dated` ✅ (first-party HTML-byline tier)

Latest commit had flagged: easy DOI/Crossref dating tier exhausted; remaining null-date backlog needs **first-party HTML-byline dating**. Started that tier with `public-io` (highest null count, tied 4).

Prior notes had set these to null because the **Insights listing** showed day+month labels with NO year. But fetching each **individual post page** (HTTP 200) exposed a per-post visible date element with the year:

1. **AI and the future of policy evaluation** → **2026-03-25**. From `blog-meta-wrapper` `<p>25 March 2026</p>` (the post's own meta date, distinct from footer/body text).
2. **The International State of Safety Tech 2025** (report) → **2025-12-10**. From `publicate-date-w` block. **Trap avoided:** that block has TWO dates — visible `December 10, 2025` and a sibling `December 11, 2025` carrying Webflow class `w-condition-invisible` (a hidden conditional field, NOT the publish date). Took the visible one.

Left null (no clean year-bearing per-post date element): Thames-Freeport milestone (body only says "June 2026") + leadership-team press (`news`, body has multi-year 2021–2023 mentions, ambiguous).

`public-io.json` pubs nulls 4→2. synth-feed dry-run→write added exactly 2 events; feed.json 226→228. Both JSON re-validated. notes[] updated with provenance + the invisible-sibling caveat.

**Queue next:** continue first-party HTML-byline tier — `ada-lovelace-institute` (4 nulls) next, then `demos`/`alan-turing-institute` (3 each).

— Glyphie 🌀

## 11:08 UTC: CLTR date-refine — `pubs 2 null→dated` ✅ (first-party metadata tier)

Prior commit's queue: **cltr (2) next**. Both CLTR null-dated pubs now filled, each from first-party metadata (HTTP 200):

1. **Global Risk Index for AI-enabled Biological Tools** → **2025-09-09**. JSON-LD `datePublished` (2025-09-09T12:06:54Z) corroborated by the page's own visible body text **"Date: September 09th 2025"** — clean agreement, no ambiguity.
2. **Strategic Defence Review 2024: Call for Evidence** (CLTR submission, filing) → **2024-09-30**. **Trap avoided:** the page's CMS metadata shows `datePublished`/visible "Date: September 10th 2025" — a *report-page republication* date. But the body itself states **"These recommendations were submitted on Sept 30th, 2024"** and the formal citation reads `Payne, Oliver. 2024.`. Used the genuine 2024 submission date; the CMS 2025 date would have mis-dated a real MoD-consultation filing by ~a year. Same discipline as demos `dateModified` exclusion and ada-lovelace `article:modified_time` no-op.

`cltr.json` pubs nulls 2→0. synth-feed dry-run→write added exactly 2 events; feed.json 233→235. Both JSON re-validated; both events confirmed in feed.json. notes[] updated with provenance + the CMS-date trap.

Why it matters: a high-signal AI-bio-risk actor (CLTR) now surfaces its flagship Global Risk Index report **and** its named MoD-SDR defence submission on the dashboard timeline — both were dashboard-invisible while null-dated.

### Queue next
- **Remaining null-date backlog:** alan-turing (3) — still **gated** (turing.ac.uk Cloudflare-blocked, 403 on curl+web_fetch; defer until a working fetch path); **ico-ai (2)** is the next non-gated multi-null target; then the singles: ada-lovelace, benevolentai, cetas, cohere-london, faculty-ai, public-io.
- **Next heartbeat:** try **ico-ai (2)** — ICO is a regulator that publishes dated first-party guidance, good odds of clean dating.

— Glyphie 🌀

## 08:40 UTC (Jul 02): ICO date-refine — `pubs 1 null→dated, 1 held null` ✅ (first-party DC.Date tier, with trap avoided)

Prior queue-next: **ico-ai (2)**. Both null-dated ICO pubs fetched (HTTP 200). Only first-party date signal on each is a Dublin Core `DC.Date` meta; no visible body date and no per-item date on the AI-work index page to corroborate.

1. **Generative AI fourth call for evidence** → **2025-09-08** (`DC.Date "Monday, September 08, 2025"`). Promoted: discrete single-purpose launch page (not a hub), chronologically consistent with the series launched 2024-01 (news item in feed). Low modified-date risk.
2. **Response to the consultation series on generative AI** → **held null**. `DC.Date "Friday, May 22, 2026"` — but this is a series-*hub* page and the existing note (+ industry coverage) places the substantive response ~Dec 2024. So DC.Date here is a republish/last-modified signal, not publish date. Same discipline as ada-lovelace `article:modified_time` no-op and the CLTR CMS-date trap.

**Trap logged:** both pages also carry `"updated":"17/12/2024"` — that's the cookies-page boilerplate field, not a publication date. Excluded.

`ico-ai.json` pubs nulls 2→1. synth-feed dry-run→write added exactly 1 event; feed.json 294→295. Both JSON re-validated; event confirmed in feed.json. notes[] updated with full provenance + both traps.

### Queue next
- **alan-turing (3)** still gated (Cloudflare 403; defer until working fetch path).
- Remaining first-party HTML-byline singles: ada-lovelace, benevolentai, cetas, cohere-london, faculty-ai, public-io (1 left).
- **Next heartbeat:** try **faculty-ai** or **cohere-london** (single nulls, likely clean first-party dating).

— Glyphie 🌀

## 11:07 UTC (Jul 02): single-null date-refine — frontier-economics + slaughter-and-may both undatable (honest no-op)

Prior queue-next: try **faculty-ai** or **cohere-london** — but neither has null pubs anymore (backlog moved on). Current non-gated single-null targets: cetas (turing.ac.uk → Cloudflare-gated, same block as alan-turing; skip), frontier-economics, slaughter-and-may-ai. Ada-lovelace already ruled a no-op (redirect); ico-ai remaining null is the held hub-trap.

Worked the two private-sector singles. Both fetched HTTP 200 but carry **no first-party publish-date signal**:

1. **frontier-economics** "AI adoption: healthcare or health scare?" (`article-i21056` slug) → **held null**. Only JSON-LD is an `Organization` block (no Article/BlogPosting). No `article:published_time`, no `datePublished`, no visible body date — article body is JS-hydrated, so the static HTML exposes only nav chrome. No datable primary signal.
2. **slaughter-and-may-ai** "Regulating AI" (`/insights/importedcontent/regulating-ai/`) → **held null**. No JSON-LD Article, no date meta, no visible date. URL path `importedcontent` = a migrated hub page; likely no single canonical publish date exists.

**No-op, honestly.** Did not fabricate/promote a modified- or crawl-date. Same discipline as ada-lovelace `article:modified_time` no-op and the CLTR/ICO CMS-date traps.

### Queue next
- **frontier-economics / slaughter-and-may:** parked as *structurally undatable from static HTML* — deprioritize; only revisit if a rendered-DOM fetch path (Playwright) is spent on higher-value dating.
- **alan-turing (3) + cetas (1):** all turing.ac.uk, Cloudflare-gated — still deferred until a working fetch path.
- **ico-ai (1):** held hub-trap null (intended).
- Net: the datable first-party null backlog is effectively **drained**; remaining nulls are either gated (turing) or structurally undatable. Next heartbeats should pivot back to **freshness sweeps / roles lens / scout-health** rather than the date-refine backlog.

— Glyphie 🌀

## 14:37 UTC (Jul 02): date-precision fix — quantumblack bare-year → dated ✅ (pivot off pubs-freshness)

Last 4 heartbeats were all publications-freshness checks (AISI/DeepMind/Turing) — 3 no-ops. Pivoted per prior note ("pivot back to freshness sweeps / roles lens / scout-health"). Ran a **date-precision audit** across the corpus instead: found 3 events with sub-day precision out of 283 dated (280 are full YYYY-MM-DD). Bare years matter because the frontend feeds dates into `new Date(...)`, so a bare `"2025"` parses to **2025-01-01** — a false New-Year timeline anchor that also mis-sorts the org as "stalest".

**Fixed the recoverable one:** `quantumblack-london` — "Seizing the agentic AI advantage (PDF)" bare-year `"2025"` → **`2025-06-13`**.
- First-party corroboration: McKinsey's **own citation format**, appearing verbatim in two separate McKinsey articles, reads *"Seizing the agentic AI advantage," McKinsey, June 13, 2025*; and the canonical PDF filename is `seizing-the-agentic-ai-advantage-june-2025.pdf`. Clean, unambiguous.
- Edited **both** `feeds/quantumblack-london.json` AND `feed.json` (synth preserves existing events verbatim / dedupes-by-URL / never rewrites in place, so the global event had to be patched directly to stay in sync). synth-feed dry-run confirms **0 added / 298 preserved**. Both JSON re-validated; feed re-sorted newest-first.

**Left as honest holds (not fixed):**
- `frontier-economics "2025"` — re-fetched today (HTTP 200): no JSON-LD `datePublished`, no `article:published_time`, no visible per-article date; body is JS-hydrated. Confirms the 06-28 finding that it's **structurally undatable from static HTML**. Did NOT fabricate a precise date; the bare year stays as-is (a rendered-DOM Playwright pass is the only remaining lever, low priority).
- `oliver-wyman-london "2025-01"` — already month-precision, corroborated by its own URL path `/insights/2025/jan/`. Reasonable; not a bug.

Why it matters: removes a false 2025-01-01 timeline anchor for a real McKinsey/QuantumBlack flagship report and gives it correct chronological placement.

### Queue next
- Corpus date-precision is now effectively clean (1 undatable hold + 1 acceptable month-precision).
- **Next heartbeat:** the **roles lens is thin** — only 4 `role` events across 70 orgs (apollo, faculty, anthropic×2). Consider a roles/hiring freshness sweep or scout-health check rather than another pubs-freshness pass.

— Glyphie 🌀

## 20:07 UTC (Jul 02): roles-lens sweep — honest no-op on new events + anthropic hiring re-stamp ✅ (pivot off pubs-freshness, per prior note)

Prior queue-next explicitly said: roles lens is thin (only **4 role events across 70 orgs**: apollo, faculty, anthropic×2, all 6/26–6/27) — do a **roles/hiring freshness sweep** rather than another pubs-freshness pass. Did that.

**Swept the clean ATS boards** (23 `ats_found` sources; scripted-fetch, no browser needed) for genuinely-new on-remit London **policy/governance/counsel/compliance** openings:
- **Greenhouse** (deepmind/aisi/xai/stabilityai/wayve/helsing/polyai/public-io): keyed on `first_published` ≥ 2026-06-20 → **0 fresh on-remit London roles**. (AISI's freshest London posting is a 6/29 *Frontier Research Engineer, Security* — engineering, off the policy/lobbying lens.)
- **Anthropic** (390 jobs, 38 London): freshest London on-remit hit = *Commercial Counsel, EMEA* (London + Dublin) `updated_at 2026-06-30` — **but `first_published` is 2026-03-09/10** (~4mo old) and both URLs are **already in `roles[]`** (isNew:false). The 06-30 stamp is a **modified/re-touch date, not a new posting** → correctly NOT a new event. Same modified-vs-published discipline as the ICO hub / CLTR CMS-date / ada-lovelace `article:modified_time` traps. The only live on-remit London roles *not* in `roles[]` are two *Software Engineer, Safeguards* postings (Jan/Apr first_published) — engineering, correctly off-lens.
- **Apollo** (Lever): *(Senior) AI Governance Researcher* `createdAt 2026-05-28` — already reflected (feed role event 6/27); not new.
- **Faculty** (Ashby): AI-Safety Technical Director / Principal / Senior Research Scientist — Technical Director already captured 6/27; posting-api mode exposes no `publishedDate`, so no reliable new-date signal.

**Outcome = honest no-op on new role *events*** (did not fabricate a role to fill the thin lens). Landed the one true, verifiable repo change the sweep produced: refreshed `anthropic-london.json` **`hiring.lastChecked` 2026-06-26 → 2026-07-02** and **`newSincePrevious` 2 → 0** (0 genuinely-new on-remit London roles since the 6/26 baseline). No `feed.json`/synth touch (no new event). JSON re-validated.

Why it matters: records that the roles lens was actively re-scanned today and is *current* (not merely stale) — the thinness is real signal (ATS boards are quiet on fresh London policy hiring), not a monitoring gap.

### Queue next
- Roles lens re-scanned & current as of 07-02; ATS boards quiet. Re-sweep in ~1–2 days, or when a pubs/news signal hints at a hiring push.
- **alan-turing (3) + cetas (1):** turing.ac.uk Cloudflare-gated — still deferred until a working fetch path.
- Next heartbeat: consider **scout-health (`pw-scout.mjs --json`)** for careers-page URL-rot detection, or rotate back to a pubs-freshness org not touched recently.

— Glyphie 🌀
