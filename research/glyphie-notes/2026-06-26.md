# 2026-06-26 — Glyphie daily note

_Fatima asked for two follow-ups to the roles lens: (1) fold the roles snapshot into the per-company feeds so they're DB-ready, and (2) propose the `feeds-sync` extension so `roles[]` + `controversies[]` actually reach the dashboard. Both in progress this session._

## 1. Roles → feeds fold-in (research/, self-merge)

Re-ran `fetch-roles.mjs` for a live snapshot: **89 policy roles → 26 London/EMEA, 8 new since the 06-24 baseline** (the delta now works). Folded the **London/EMEA-relevant** roles into per-company feeds:

- **Updated** existing feeds: `anthropic-london` (10 roles, 2 new — both **Policy Counsel, EMEA**, London + Dublin), `elevenlabs-london` (1), and the snapshot also covers openai/wayve but those had 0 London-relevant policy roles so no change.
- **Created** roles-only feed files for covered companies that had London policy roles but no feed yet: `faculty` (3× AI-Safety scientist, London), `helsing` (Legal Counsel, London), `mistral-london` (8× legal counsel, Paris/EMEA), `synthesia` (Product Counsel, London), `xai-london` (Legal Counsel, London + remote). These are marked `[roles-only feed]` in `notes` — publications/controversies not yet researched for them.
- **`hiring` block** per feed now carries `isHiring`, `newSincePrevious`, `londonPolicyRoles`, `lastChecked`.
- **`feed.json`**: added the **2 new** London roles as `event_type: "role"` timeline events (only *new* + London-relevant roles hit the timeline — I don't flood it with all 26 standing roles; the delta is the signal).

Discipline note: roles[] entries are tagged `source: "ats"` (public ATS API, not hand-curated) and the new minimal feeds are honestly labelled so nobody mistakes a roles-only stub for a fully-researched company.

## 2. feeds-sync extension (src/, Lotus's domain → PR for her review)

`feeds-sync` currently only ingests `publications[]`. The `roles[]` and `controversies[]` I write never reach the dashboard. Drafting the sync extension as a **separate PR** (touches `src/`, outside my self-merge scope) and tagging Lotus in #lobby-cat for review. See that PR for the table/upsert shape — it mirrors the existing `publications` pipeline (companyId + url unique index, idempotent upsert).

## Carried forward

- ElevenLabs Illinois BIPA docket number — still want the case number.
- Ofcom Grok NCII investigation — still single-aggregator-source, NOT logged.
- Unmapped roles companies (think tanks/regulators, bespoke-HTML careers) — no ATS API; future careful-HTML-read.
