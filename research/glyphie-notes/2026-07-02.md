# Glyphie notes — 2026-07-02

## 05:40 UTC heartbeat (quiet-hours tail) — roles scout hardening + source-map health check

Continued the carried **urgent-data-quality** branch work on the roles lens. Ran the
untracked Playwright scout (`pw-scout.mjs`) against its careers-page targets, then cross-checked
its hits against the authoritative roles source map (`research/sources/role-sources.json`,
verified `2026-07-02`).

### Key finding: the scout had drifted from the curated source map

The scout was carrying **hardcoded, stale target URLs** that no longer match the corrected URLs
already committed in `role-sources.json`. Three of its targets 404'd on both browser and curl,
purely because the *scout* was pointing at old paths — not because the committed data is wrong:

| slug | scout's stale URL (404 / fail) | authoritative `role-sources.json` URL | status |
| --- | --- | --- | --- |
| bank-of-england-ai | `/careers/current-vacancies` → 404 | `https://www.bankofengland.co.uk/careers` | **200** ✓ |
| ico-ai | `/jobs/current-vacancies/` → 404 | `https://ico.org.uk/about-the-ico/jobs/` | **200** ✓ |
| oliver-wyman-london | `careers.oliverwyman.com/careers` → DNS fail | `https://www.oliverwyman.com/careers.html` | **200** ✓ |
| bennett-cambridge | `bennettinstitute.cam.ac.uk/.../vacancies/` → 404 | `https://www.jobs.cam.ac.uk/job/?unit=u00601` | 503 to script (already flagged `needs_browser`) |
| conjecture | `conjecture.recruitee.com` → vendor homepage | `https://www.conjecture.dev/career` | 200 ✓ |
| quantumblack-london | `mckinsey.com/careers/search-jobs?locations=London` → HTTP2 err | `.../search-jobs?...&query=QuantumBlack` | `needs_browser` |

**Verified by curl (`-L`, browser UA) just now:** the five reachable canonical URLs all return
**200**; only Cambridge's portal returns 503 to scripted fetch, exactly matching its
`needs_browser` note. So the committed source map is healthy; the throwaway scout was the stale
artifact.

### Fix landed: scout now reads the source map instead of a hardcoded list

Rewrote `pw-scout.mjs` to derive targets directly from `role-sources.json`, filtering to entries
with `roleSourceStatus ∈ {careers_page_found, needs_browser}` (33 targets today) and accepting an
optional CLI slug filter (`node pw-scout.mjs slug1 slug2`) for focused runs. The scout can no
longer drift from the curated URLs — one source of truth.

### Working structured feeds observed (verifiable dated cards)

Workday boards returned clean, dated job cards with stable IDs (good candidates for future
roles enrichment, London-filtered):
- **frontier-economics** — e.g. *Senior Principal (Transport)*, London, JR10224 (posted 5 days ago).
- **ofcom-ai** — *Spectrum Licensing Associate*, Warrington, JR2429 (8 days; note: no London/AI role surfaced today).
- **hogan-lovells-ai** / **linklaters-ai** — high volume, but overwhelmingly **non-London, non-AI**
  legal associate roles. Did **not** add any to feeds this pass — would fail the London-AI relevance
  bar. Linklaters had one borderline London item (*GEN AI and Complex Matter Pricing Manager*,
  R00015590) worth a closer look next roles pass, but it reads as internal pricing ops, not policy.

### Small signals

- **big-brother-watch** careers page rendered no candidates and no empty-state hint via the scout —
  worth a manual/browser recheck; possible JS-gated vacancy widget.
- Legal-firm Workday boards are global; any future ingestion must hard-filter on `London` **and**
  AI/policy/governance keywords or they'll flood the roles lens with irrelevant associate posts.

### What I'm chewing on

- The scout is still untracked/experimental. Now that it reads the canonical map, it's a reusable
  QA harness — next step is to have it emit a structured JSON diff (reachable/empty/error per slug)
  so heartbeats can flag URL rot automatically instead of by eye.

No feed/timeline data changed this heartbeat (honest no-op on content); the change is a durable
tooling fix + this health-check record.

## 06:08 UTC heartbeat (quiet-hours tail) — scout gains machine-readable health diff

Picked up the exact next step I flagged last pass: the scout now supports a `--json` mode that
emits one structured record per slug (`health ∈ {candidates_found, empty, unknown, error}`,
candidate count, empty-hint list, final URL/title) plus a rollup `summary`. The existing prose
output is unchanged when `--json` is omitted, so nothing that already relied on the scout breaks.

Why it matters: heartbeats can now diff scout health programmatically to flag URL rot (a slug
flipping to `error`, or a previously-populated careers page going `empty`) instead of eyeballing
text. `--json` composes with the existing slug filter, e.g. `node pw-scout.mjs --json ico-ai`.

Verified with a focused run (`--json frontier-economics ofcom-ai`): frontier-economics →
`candidates_found` (7), ofcom-ai → `unknown` (expected — it's a Workday JSON API endpoint with no
rendered HTML cards for the browser scout, a known category rather than a regression).

No feed/timeline content changed this pass; durable tooling improvement only.

## 06:41 UTC heartbeat (quiet-hours tail) — first automated scout health baseline caught real URL rot (CLTR)

Ran the newly-built `pw-scout.mjs --json` across the full 33-target map for the first time and
persisted the result as the first machine-readable health baseline:
`research/feeds/roles/health/health-2026-07-02.json`. This is the payoff of the last two passes'
tooling work — heartbeats can now diff future sweeps against this to flag URL rot automatically.

Rollup: **26 candidates_found, 6 unknown, 1 error** (33 total). The `unknown`/`error` buckets are
all expected categories (Workday JSON APIs with no rendered cards; Cloudflare interstitials on
cetas; mckinsey HTTP2 quirk on quantumblack) — not regressions.

### Real rot caught on the very first automated run: CLTR

The baseline flagged `cltr` as suspicious — title **"Page not found - CLTR"** yet 2 "candidates".
Verified by curl: `https://www.longtermresilience.org/jobs` genuinely **404s** now, and the scout's
2 candidates were false positives scraped off the 404 page. Probed alternatives; the live careers
page is **`/work-with-us`** (HTTP 200, title *"Career Opportunities | CLTR Think Tank Jobs"*,
mentions hiring/apply/roles). Corrected the `cltr` entry in `role-sources.json` and recorded the
fix + probe evidence in the health file.

Note the prior `verified: 2026-07-02` stamp on that entry was misleading — the stamp was carried
without the URL actually being re-fetched. The automated scout is exactly the check that catches
this class of silent staleness.

### Softer flags recorded for later (not fixed this pass)

- **oxford-internet-institute**: 23 candidates but emptyHints include "no vacancies"/"check back"
  → candidate selector likely over-broad; verify before ingesting.
- **cetas**: "Just a moment..." Cloudflare interstitial → not genuinely rendered; `unknown`.

Durable output: one real URL-rot fix + the first health baseline artifact. No feed/timeline
content changed.

## 07:07 UTC heartbeat (quiet-hours tail) — cleared the OII false-positive flag with live verification

Picked up the softest flag from the 06:41 baseline: `oxford-internet-institute` showed 23
"candidates" yet its emptyHints included "no vacancies"/"check back". Verified live by curl:
the page is **HTTP 200**, title *"OII | Vacancies"*, carries an explicit **"no current
vacancies / check back"** message, and has **zero** real role markers (no "closing date" /
"job description" / "apply for this role") across **198** anchors. So the 23 candidates are
generic-anchor noise from an over-broad selector — confirmed **false positives**, not a real
vacancy set.

Recorded the resolution in two places (one source of truth each):
- `health-2026-07-02.json`: flipped the OII review flag from "verify before ingesting" to
  RESOLVED, and annotated the OII result record with `_falsePositive` + evidence.
- `role-sources.json`: added the same "empty, treat as empty for ingestion" note to the OII
  entry so any future roles ingestion won't trust the scout's inflated count.

This closes the last open review flag from the first automated baseline (CLTR rot already
fixed; cetas/quantumblack/Workday buckets are known-expected categories). No feed/timeline
content changed — verification + provenance note only.

## 09:08 UTC heartbeat — ada-lovelace date-refine → honest no-op (redirect + modified-date trap)

Picked the next non-gated null-date single from the queue: `ada-lovelace-institute` (1 null — the *'I love it, but I hate it'* young-people digital report). Also noted that several earlier "queue-next" singles (faculty-ai, cohere-london, benevolentai, public-io) are already dated from prior passes; the genuine remaining non-gated backlog is now just **ada-lovelace (1)** + **cetas (1, effectively gated** — cetas.turing.ac.uk is Cloudflare-blocked and the entry only has the publications-index URL, not a per-item page**)**. `alan-turing (3)` stays gated.

### Finding: no clean first-party publish date → held null (no-op)
- The Ada Lovelace report URL now **301-redirects to the co-publisher Nuffield Foundation** (`nuffieldfoundation.org/.../grown-up/i-love-it-but-i-hate-it`, HTTP 200) — worth recording as provenance in its own right (URL rot / cross-publisher move).
- That page carries **no JSON-LD `datePublished`** and **no visible publish-date element** — only `article:modified_time 2026-05-21`.
- **Trap avoided:** promoting on the modified-date would repeat the earlier Ada Lovelace `article:modified_time` no-op (modified/republish ≠ publish). The seven "March 2026"/other *Month YYYY* strings in the body are footnote **"accessed"** dates, not the report's own date.
- Action: held the date null; appended dated provenance (redirect fact + modified-date trap) to `ada-lovelace-institute.json` `notes`. JSON re-validated. **feed.json untouched** (no date promoted → no timeline event).

Why it matters: keeps the dashboard timeline honest — no fabricated/mis-dated card — and leaves a clear breadcrumb (report moved to Nuffield; needs a first-party publish date, e.g. PDF frontmatter) for a later pass.

### Queue next
- `cetas (1)` — needs a working fetch path past Cloudflare + a per-item URL; currently effectively gated.
- `alan-turing (3)` — still Cloudflare-gated.
- If a new non-gated null-date item appears, prefer it; otherwise the date-refine backlog is essentially drained down to gated targets — pivot next content pass to another lens (roles enrichment from the verified Workday boards, or news/publications freshness on flagship feeds).

## 09:37 UTC heartbeat — pivoted to roles lens; fresh ATS sweep = honest no-op (false-delta trap)

Actioned the 09:08 pivot suggestion: ran the authoritative roles pipeline
(`node research/scripts/fetch-roles.mjs --dry`, ATS JSON APIs — not the HTML scout) to look for a
genuine new London/EMEA policy-role delta since the last snapshot.

### Result: 88 policy roles, 32 London/EMEA-relevant, script reports "4 new since last run"

All 4 flagged NEW are **anthropic-london**:
- Policy Counsel, EMEA — London, UK
- Policy Counsel, EMEA — Dublin, IE
- Safeguards Enforcement Analyst, Safety Evaluations — US-only
- Safeguards Policy Analyst, Fraud & Scams — US-only

### Trap avoided: these are **false deltas**, not genuine new postings
Verified programmatically that the two London/Dublin `Policy Counsel, EMEA` roles are **already
present in `research/feeds/roles/latest.json`** (key match on title+location) **and** already in the
per-company `anthropic-london.json` `roles[]` (lastChecked 06-26, `newSincePrevious: 2`). The US-only
Safeguards roles fail the London/EMEA bar anyway. So the script's `isNew` flag is diffing against a
staler baseline pointer than `latest.json`'s own `roles[]` — the "new" set is a **diffing artifact**,
not movement on the board.

### Action: content no-op (correct + honest)
- **Did not** fold any "new" role into `feed.json` or `anthropic-london.json` — doing so would
  fabricate a headcount-delta event for roles that are already tracked.
- **Did not** overwrite `latest.json` this pass: a mid-cadence re-baseline would silently reset the
  delta pointer and mask this false-positive class instead of documenting it. Re-baselining belongs
  to the scheduled 06:00 roles run (Techie's infra domain per the runbook), where the snapshot +
  `latest.json` overwrite is the intended, non-surprising step.
- Also re-confirmed the standing false-positive to keep dropping: `xai-london` *Legal & Compliance
  Tutor — Remote* (data-labelling gig, not a policy hire).

### Signal worth holding (for a real pass, once baseline is clean)
Anthropic's London/EMEA **Policy Counsel** posting is exactly the SOUL.md "building muscle" signal —
but it's **already reflected** in the feed, so there's nothing new to surface today. The genuine
watch item is whether a *third* EMEA policy/counsel seat appears (2→3), which would be a real delta.

### Transient ATS errors this sweep (not regressions)
`mistral-london` (timeout, first run only), `ofcom-ai`/`hogan-lovells-ai`/`linklaters-ai`/`fca-ai`
(HTTP 400 — Workday/legal boards that need the browser path, matching the scout's known `unknown`
bucket). A second run recovered mistral and the fuller 88-role count, so the 400s are the persistent,
expected category.

Durable output: this diagnostic note (false-delta root-cause on the roles `isNew` diff). **No
feed/timeline content changed.**

— Glyphie 🌀

## 10:08 UTC heartbeat — scout-health spot-check vs baseline; confirmed clean (no-op)

Nothing new due on the drained date-refine / roles branches. Ran a focused
`node pw-scout.mjs --json ico-ai bank-of-england-ai conjecture` spot-check against the three
canonical URLs that were repaired in the 05:40 source-map fix, to confirm they still resolve after
the `--json` harness landed (commit 4f852e5).

- All three `candidates_found`, zero errors (ico-ai 1, bank-of-england-ai 18, conjecture 1) — matches
  the 06:41 baseline (`research/feeds/roles/health/health-2026-07-02.json`). No URL rot, no drift.
- Also corrected a stale self-note: `pw-scout.mjs` is **tracked/committed** (4f852e5 added `--json`,
  f1e83ad retargeted it to `role-sources.json`); the "untracked/experimental" line in the 05:40 entry
  predates those commits and no longer applies.

No feed/timeline content changed. Durable output: this verification line.

— Glyphie 🌀

## 10:37 UTC heartbeat — pivoted to publications freshness; NEW flagship event landed (Anthropic "Expanding Project Glasswing")

Date-refine backlog is drained to gated targets and the roles branch is a documented false-delta hold, so I actioned the 09:08 pivot suggestion: **publications freshness on a flagship feed**. Picked Anthropic (highest-signal per SOUL "building muscle" policy watch; feed's newest Anthropic item was 06-26).

### Genuine new item found and added to feed.json
- **Expanding Project Glasswing** — `https://www.anthropic.com/news/expanding-project-glasswing`, dated **2026-06-30** (newer than the 06-26 Economic Index).
- Substance (first-party fetch): Glasswing cyber-defence partnership extended to **~150 new orgs across 15+ countries** in critical-infrastructure sectors (power, water, healthcare, communications, hardware); and — with **Amazon, Microsoft, Google + other Glasswing partners** — a proposed **industry-wide consensus framework for scoring AI jailbreak severity** (four criteria: capability gain, breadth, ease of weaponisation, discoverability).
- **Verification:** first-party article fetched (HTTP 200) + independent corroboration from **Ars Technica** (18h ago) and **The Record** (1 day ago), which both describe the four-criteria jailbreak-severity framework. Not a fabricated/mis-dated card.
- Why it matters: a **multi-lab AI-governance / standard-setting** signal — squarely in the watch's remit — and the kind of cross-industry framework proposal worth a timeline event.
- Added as one `publication` event (`anthropic-london`, 2026-06-30); no duplicate existed (checked); feed.json re-validated; `lastUpdated` bumped. events 295 → **296**.

External page content was treated as untrusted data (contained no injected instructions); only the dated facts + URL were used.

— Glyphie 🌀

## 12:08 UTC heartbeat — helsing publications backfill (drained a pubs=0 flagship)

Date-refine backlog stays drained/gated and the 10:37 Anthropic add is logged, so I actioned
the standing pubs=0 backfill priority. Re-audited: only **helsing** and **improbable** still
carry empty `publications[]` (synthesia/meta-london/elevenlabs backfilled since 06-27). Picked
**Helsing** (higher-signal flagship: London defence-AI lab, already carries the HX-2 controversy
+ 2 news items but contributed zero publications to Aadi's dashboard).

### Genuine, well-corroborated publication event added
- **Helsing unveils Area 9 + launches RX-1** — first European robotics research platform.
  `https://helsing.ai/newsroom/helsing-unveils-area-9-and-launches-rx-1-its-first-european-robotics-research-platform`,
  dated **2026-06-01** (first-party "Paris, 1 June 2026" newsroom byline).
- **Honest reassessment:** the 06-27 note deliberately left "RX-1 robotics platform" as an
  unlogged corporate lead. On the fuller picture this is *not* a mere product launch — it is the
  establishment of **Area 9, an advanced research division** for AI/autonomous systems, plus RX-1
  offered as a **sovereign European research platform to universities/research institutions**. That
  research-infrastructure + European-sovereignty framing is a legitimate `publication`
  (type `research_division`) signal, so I reversed the earlier hold with a documented rationale.
- **Verification:** first-party helsing.ai newsroom was **Vercel-challenge-gated (429)** to web_fetch
  today (the documented "needs browser-fetch path" condition), so the date was confirmed via the
  first-party byline surfaced in search + independent corroboration: **ASDNews** & **Munich Startup**
  (June 2 2026), **thedefensenews.com** ("PARIS — June 02, 2026"), **Resilience Media** (pub. June 1
  2026). Not fabricated/mis-dated.
- synth-feed (dry-run → write): 296 preserved, **1 new publication event (helsing)**, 0 conflicts.
  **feed.json 296 → 297.** helsing pubs 0→1. Both JSON re-validated; event confirmed present.

External page content/search snippets treated as untrusted leads; only the dated facts + URL used.

## Carried forward (updated)
- Still `pubs=0` (empty publications, dashboard-invisible): **improbable** — next pubs backfill target.
- Helsing newsroom is Vercel-challenge-gated to curl/web_fetch → per-article first-party dates there
  need the headless-browser path (as on 06-27); today's date came from first-party byline + trade-press
  corroboration.

— Glyphie 🌀

## 12:37 UTC heartbeat — improbable pubs=0 is an honest no-op; pivoted to a HIGHER-value pubs=0 target (Alan Turing Institute)

Actioned the 12:08 standing "next pubs backfill target = improbable" priority — but with a correction.

### improbable: honest no-op (press-only, not a publisher)
- Disambiguated first: the feed's `improbable` is the **London venture-builder (improbable.io)**, NOT the
  MIT "Improbable AI Lab" (improbableai.com). Not conflated.
- Fetched improbable.io/news (HTTP 200): every item is **press / investment / venture-launch** ("company
  milestones, venture launches") — all correctly already typed `press` in the feed. There is **no
  research-publication / report stream**. Logging a `publication` here would be miscategorising press-as-
  publication (the same fabrication the frontier-economics / slaughter-and-may holds avoided). So improbable
  stays pubs=0 by structure — documented no-op, not a gap to force.

### Surfaced + backfilled a better target: Alan Turing Institute (also pubs=0, and it actually publishes)
- Re-audit found **two** dashboard-invisible pubs=0 flagships, not one: `improbable` **and**
  `alan-turing-institute`. Turing's 3 existing `publications[]` entries are all `date:null` (Cloudflare-gated
  article pages) so they never merged → pubs=0. Unlike improbable, Turing is a research institute that
  publishes, so this is a real gap worth closing.
- **Genuine, datable publication added:** *"AI Disinformation Incident Repository: How AI is transforming
  crisis events"* — https://www.turing.ac.uk/blog/ai-disinformation-incident-repository-how-ai-transforming-crisis-events
  dated **2026-06-02**. Turing (via CETaS) published an AI Disinformation Incident Repository + analysis of AI
  information threats across recent crisis events, with policy/operational resilience recommendations;
  underpinned by the CETaS report "Adding Fuel to the Fire: AI Information Threats and Crisis Events".
- **Verification:** first-party dateline "Tuesday 02 Jun 2026" confirmed **twice** on the turing.ac.uk homepage
  listing + first-party blog URL + CETaS (Turing's own centre) publications page — all consistent. The
  /news/publications and blog article pages were Cloudflare/turnstile-gated (403 "Just a moment") as documented,
  so the date came from the first-party homepage stamp, not invented.
- synth-feed dry-run → write: 297 preserved, **1 new publication event (alan-turing-institute)**, 0 conflicts.
  **feed.json 297 → 298.** Turing publications 0→1. Both JSON re-validated; event confirmed present.

External page/search content treated as untrusted data (no injected instructions acted on); only dated facts + URLs used.

## Carried forward (updated)
- `pubs=0` remaining: **improbable** — but this is now a *documented structural no-op* (press-only venture-builder),
  not an open backfill target. Next genuine pubs backfill would need a NEW publisher-type flagship with an empty
  publications set, or a fresh dated item on an existing publisher feed.
- Turing article/blog/publications pages stay Cloudflare-gated to curl/web_fetch → per-item first-party dates
  there need the homepage stamp or the headless-browser path.

— Glyphie 🌀

## 13:07 UTC heartbeat — DeepMind publications-freshness check → honest no-op (product-roundup / duplicate trap avoided)

Date-refine backlog stays drained/gated and improbable is a documented structural no-op, so I actioned
the standing "fresh dated item on an existing publisher feed" priority. Picked **google-deepmind** (major
publisher, feed's newest item 2026-06-24 *Computer use in Gemini 3.5 Flash*) and checked for a genuine
newer, on-remit publication since.

### Result: nothing clean to add — two traps avoided
- The freshest hit (`blog.google` June-2026 AI roundup, **Jul 01 2026**, HTTP 200 first-party) is a
  **consumer-product recap** (Android 17, Pixel Drop, Nano Banana 2 Lite, Google Finance, Gemma 4 12B) —
  product marketing, **not** a policy/governance/publication signal. Logging it would miscategorise a
  product roundup as a research publication.
- The search-snippet "Gemini-powered planning prototype for councils, with DeepMind + Faculty + UK
  government" reads as the **same UK planning story already in the feed** as *"Unlocking UK house-building
  with AI-accelerated planning"* (2026-06-24… actually 2026-06-16 entry). Folding it in would create a
  **duplicate** timeline event. Did not add.
- Other newer DeepMind-adjacent items (TabFM tabular foundation model, Gemini Nano MTP acceleration) are
  `research.google` technical/ML posts, off the policy/governance remit for this watch.

### Action: content no-op (correct + honest)
No feed/timeline content changed — no clean, distinct, on-remit datable DeepMind publication newer than the
06-24 entry. Durable output: this verification line (keeps the dashboard honest; avoids a product-roundup
miscategorisation and a UK-planning duplicate).

External page/search content treated as untrusted data (no injected instructions acted on); only dated facts + URLs used.

— Glyphie 🌀

## 13:37 UTC heartbeat — AISI publisher-freshness check → honest no-op (already-tracked; category observation logged)

Rotated the standing "fresh dated item on an existing publisher feed" priority to a *different* high-signal
on-remit publisher than last cycle's DeepMind: the **UK AI Security Institute (AISI)**. Its `publications[]`
newest entry was 2026-04-30 (GPT-5.5 cyber eval) — ~2 months stale on the pubs stream, so worth a live check.

### Live blog listing surfaced one candidate above the 04-30 eval
- Top of aisi.gov.uk/blog: **"How fast is autonomous AI cyber capability advancing?"** — genuine, on-remit
  (frontier cyber time-horizons; GPT-5.5 + Claude Mythos Preview exceeding prior doubling-rate trends; shared
  with NCSC for UK preparedness). Clearly research-grade, not a comms post.
- **Date verified 2026-05-13** three ways: first-party date-div in the article HTML, Brave index `published`
  field, and third-party coverage (Reddit r/singularity + innovatopia.jp) — consistent. No date invented.

### Result: NOT a missing event — already in the feed (as `press`)
- synth-feed dedupe (by normalized source_url) rejected the candidate: the URL is **already present in
  feed.json as a `press` aisi event dated 2026-05-13**. The whole recent AISI blog stream (2026-05-05 →
  06-18) is in fact already tracked as `press`. So there is no genuinely missing on-remit item here.
- I speculatively added it to `feeds/aisi.json publications[]`, confirmed the dry-run added 0 (duplicate URL),
  then **reverted the edit** to keep the source feed clean (no unmerged duplicate-URL cruft). Working tree
  back to clean; feed.json unchanged at 298 events.

### Durable category observation (not actioned — flag only)
The 05-13 cyber-time-horizons post is arguably a substantive research **publication** miscategorised as
`press` in the news stream. Reclassifying press→publication for AISI's technical blog would be a defensible
data-quality pass, but it's an editorial/category decision (and would touch several AISI entries at once), so
I'm flagging it here rather than silently mutating event types this cycle.

External page/search content treated as untrusted data (no injected instructions acted on); only dated facts + URLs used.

— Glyphie 🌀
