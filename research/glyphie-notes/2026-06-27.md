# 2026-06-27 — Glyphie daily note

_Roles lens re-run + one genuinely new London-relevant signal folded in. Quiet, careful step — no batch-padding._

## Roles lens re-run (research/, self-merge)

Re-ran `fetch-roles.mjs` for today's snapshot: **90 policy roles → 27 London/EMEA across 15 reachable companies** (snapshot-2026-06-27.json + latest.json). The raw `newSincePrevious: 11` is noisy — most are roles already folded on 06-26 (Anthropic EMEA Policy Counsel ×2, Faculty AI-Safety ×3) re-flagged because `latest.json` was regenerated, plus several US/EU-only roles that don't pass the London filter.

After sanity-checking attribution + geo, **one genuinely new, London-relevant role** is worth surfacing:

- **Apollo Research** — *Governance Researcher (Expression of Interest)*, London & San Francisco (`jobs.lever.co/apolloresearch/...`). Apollo is an AI-safety evals org and was already wired into `role-sources.json`, but had **no feed file** until today.

### Folded in
- **Created** `research/feeds/apollo-research.json` (roles-only feed, faculty template, honestly labelled `[roles-only feed]` — publications/controversies not yet researched).
- **`feed.json`**: added the Apollo role as a `2026-06-27` `event_type: "role"` timeline event — first entry for this org.
- **Faculty**: re-verified, all 3 AI-Safety roles unchanged; bumped `hiring.lastChecked` → 2026-06-27 (no role change).

Discipline: only the *new + London-relevant* role hit the timeline. Did not re-flood it with the 27 standing roles; did not pad the new Apollo feed with un-researched publications/controversies.

## Carried forward (unchanged)
- ElevenLabs Illinois BIPA docket number — still want the case number.
- Ofcom Grok NCII investigation — still single-aggregator-source, NOT logged.
- Apollo Research — publications/controversies lens still to be researched (roles-only stub for now).
- Unmapped think-tank/regulator careers (bespoke HTML, no ATS API) — future careful-HTML-read.

**Coverage after this step:** 14 company feeds (Apollo added) — publications 18, roles 28, controversies 3, news 2.

## Mass harvest for launch (parallel sub-agent workers)

Fatima wants launch today → real coverage across the 70, not 13. Used parallel
`openclaw agent --local` workers (sub-agent path; gateway spawn unavailable in this
session) to harvest publications/news/controversy per company, each worker fetching
the org's own site and writing feed JSON via exec. Strict no-fabrication: empty array
where nothing verifiable, blocker noted, never invented.

**Roles** (scripted): expanded ATS map +5 (cohere-london, apollo-research, polyai,
improbable, oliver-wyman) → 27 London/EMEA roles. Fixed slug mismatches (faculty→
faculty-ai to match seed).

**Publications harvest:** workers + direct RSS. Landed real, URL-verified content for
the think-tank/advocacy/safety-org tier:
- TBI (6), Nesta (6), Demos (4), IPPR (3), Onward (1+2news), GovAI (3), Apollo Research
  (3+2news), Centre for Future Generations (3), Big Brother Watch (3, via RSS — Palantir/
  facial-recognition/HMRC), Foxglove (3, via RSS — Palantir/NHS/Home Office).
- All 34 sampled URLs verified resolvable (200/bot-gate); no hallucinated links.

**The ceiling (honest):** ~27 companies harvestable with web_fetch+curl+RSS. ~43 are
JS-rendered or Cloudflare-gated (most law firms, consultancies, big-tech, several
regulators — ICO/Ofcom/FCA publish AI work on bespoke pages not exposed to readability).
Workers correctly left these empty rather than fabricate. gov.uk content API returns
departments' GENERAL output (heating-oil studies on the CMA card) — rejected as too noisy.

**Coverage after this tranche:** 32 feeds, 29 with data — publications 60, news 12,
controversies 3, roles 27.

**Still needed for the gated tier:** a browser-fetch / render path (Techie's infra
domain) to crack the JS-gated ~43. Flagging to Techie. Until then, those cards rely on
a graceful empty state (Lotus's surface).
