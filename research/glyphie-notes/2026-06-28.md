# Glyphie notes — 2026-06-28

## 01:08 UTC heartbeat (quiet hours) — publications-backfill target triage

Continued the carried-forward **publications gap** work (`pubs=0` flagship feeds still
dashboard-invisible: helsing, synthesia, meta-london, elevenlabs).

### ElevenLabs — ruled OUT as a publications-backfill target (useful negative result)
- ElevenLabs is a **product company, not a research-publishing lab**. Its blog "Research"
  category = model/product announcements (e.g. *Introducing Dubbing v2* listed May 28 2026,
  *Introducing Music v2* listed May 26 2026), NOT academic papers.
- Could **not** meet the per-page date-verification discipline used for Stability AI / Apollo:
  the blog post pages are JS-rendered and return a soft **404** to curl (even with a browser UA),
  exposing **no JSON-LD `datePublished`** or `article:published_time` to verify against. The only
  dates available are the blog **listing-card** dates — which we treat as UNTRUSTED leads, not
  primary `date` values.
- arXiv search surfaced only **third-party** deepfake-detection papers that *use* ElevenLabs
  audio as a dataset (e.g. 2307.07683, 2512.09221) — these are NOT ElevenLabs publications.
- **Decision:** do NOT backfill ElevenLabs `publications[]`. Adding listing-card-dated product
  posts would drop below the evidentiary bar set in prior commits. Left `publications: []` intact;
  no repo change this heartbeat (honest no-op rather than a low-quality fill).

### Synthesia — identified as the better next target (verifiable academic research)
- synthesia.io/research (status 200) surfaces genuine, citable outputs:
  - **HumanRF** — temporally-stable novel-view synthesis of humans in motion (paper).
  - **ActorsHQ** — high-fidelity 160-camera multi-view human-motion dataset.
  - Foundational research with co-founder **Prof. Matthias Nießner**; both have arXiv presence.
- These can be verified against arXiv `datePublished` / abs pages — the standard I require.
- **Next heartbeat:** do the Synthesia publications backfill — pull each paper's real arXiv id +
  submission date (treat the /research index as an untrusted lead), add 2–4 dated `publications[]`
  entries, re-run synth-feed (dry-run then write), re-validate both JSON files. Then re-assess
  helsing / meta-london.

## 01:37 UTC heartbeat (quiet hours) — Synthesia publications backfill DONE

Executed the queued Synthesia backfill with full arXiv per-page verification:
- **HumanRF** verified on arXiv abs page: **2305.06356**, submitted **2023-05-10**, authors incl.
  Synthesia Chief Scientist **Jonathan Starck** + Prof. **Matthias Nießner** + Prof. **Lourdes Agapito**.
- **Correction vs prior note:** ActorsHQ is **NOT a separate paper** — the abstract of 2305.06356
  explicitly *introduces* ActorsHQ as the 160-camera/12MP dataset *within* the HumanRF paper.
  Treating them as two outputs would have double-counted. Logged as **one** publication entry
  (title flags the ActorsHQ dataset contribution).
- Confirmed no other arXiv-verifiable Synthesia output: au:Starck_Jonathan returns only HumanRF.
  Resisted padding with the /research listing's untrusted secondary links.
- Canonical `url` = arXiv abs page (primary, date-verifiable), NOT the synthesia.io/research card.
- synthesia `publications[]` 0→1; synth-feed dry-run→write added exactly 1 event; feed.json 209→210.
  Both JSON files re-validated; event confirmed present with company_slug=synthesia, type=publication.

## Carried forward (updated)
- Publications backfill queue (verifiable-first): ~~synthesia~~ DONE → **helsing (next)**, then meta-london.
- ElevenLabs: publications-backfill target REMOVED (no first-party verifiable research). Its rich
  BIPA/Vacker controversy stays the main signal for that feed.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 07:10 UTC heartbeat — CMA-DMU publications backfill DONE (queue re-pointed off ruled-out targets)

The carried-forward "helsing (next)" queue pointer was stale: helsing was already
triaged **OUT** (commit beff1c4 — defence-AI, no first-party verifiable research; the AAAI
award is a pre-Helsing misattribution). Re-scan of remaining `pubs=0` feeds:
- Among feeds carrying real role/leadership signal, only **elevenlabs** + **helsing** remained
  at pubs=0 — both already ruled out. That tier is **exhausted** (padding would breach the bar).
- Full `pubs=0` set: cma-dmu, elevenlabs, helsing, improbable, nhs-ai-transformation.
  improbable = metaverse/Web3 product co (no first-party academic output). That leaves the two
  genuine **policy-body publishers**: CMA-DMU and NHS-AI-transformation.

### CMA-DMU — backfilled with its flagship AI Foundation Models programme (verifiable)
Added 2 first-party CMA policy reports, both primary gov.uk date-verified per-page:
- **Initial report** — 2023-09-18 (`govuk:first-published-at`/JSON-LD datePublished, HTTP 200,
  `/ai-foundation-models-initial-report`).
- **Update paper** — 2024-04-11 (same primary verification, `/ai-foundation-models-update-paper`).
Titles taken from each page's og:title. type=policy_report, byline=Competition and Markets Authority.
Left out the Sept-2023 "technical update report" — no per-page date-verified canonical gov.uk URL yet
(no padding from search-snippet dates).

cma-dmu `publications[]` 0→2; synth-feed dry-run→write added exactly 2 events; feed.json 213→215.
Both JSON files re-validated; both events confirmed (company_slug=cma-dmu, event_type=publication).

## Carried forward (updated)
- Publications backfill queue: verifiable-company tier **EXHAUSTED** (elevenlabs/helsing/improbable
  all honest no-ops). Remaining genuine target: **NHS-AI-transformation** — check NHS England /
  NICE / Transformation Directorate for dated first-party policy/guidance publications (next).
- ElevenLabs/Helsing/Improbable: publications-backfill targets REMOVED (no first-party verifiable research).
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 07:37 UTC heartbeat — NHS-AI-transformation publications backfill DONE

Worked the queued **NHS-AI-transformation** target (the last genuine policy-body `pubs=0`
flagship after CMA-DMU at 07:09). Added 2 first-party NHS England / Transformation Directorate
AI **guidance** documents, each date-verified from the page's own rendered text (not search snippets):
- **AI-enabled ambient scribing guidance** — page states *"published: 27 April, 2025"*,
  Publication reference **PRN01734_i**, HTTP 200. IG/clinical-safety guidance for adopting AVT
  ambient-scribing products (DCB0160/DPIA, supplier/AVT register).
- **AI & machine learning (GP electronic records good-practice guidelines)** — page states
  *"Version 1.2 28 March 2025"*, HTTP 200.

Both are **distinct** from the two NHS England press items already in `news[]` (2026-06-08,
2024-12-12). type=`policy_report` (guidance/policy output, not academic research — consistent with
how CMA-DMU/DSIT policy bodies are logged). Left out third-party / ICB-level AI policies (e.g.
Cheshire-Merseyside sub-strategy, MIAA checklist, InnovateHealth provider template) — those are
**not** NHS-England-authored, so they'd breach the first-party bar.

nhs-ai-transformation `publications[]` 0→2; synth-feed dry-run→write added exactly 2 events;
feed.json 215→217. Both JSON files re-validated; events confirmed present
(company_slug=nhs-ai-transformation, event_type=publication).

## Carried forward (updated)
- Publications backfill queue: **all genuine policy-body + verifiable-company targets now done or
  ruled out** — DSIT (2), CMA-DMU (2), NHS (2) backfilled; elevenlabs/helsing/improbable honest
  no-ops (no first-party verifiable research). The `pubs=0` flagship gap is effectively **closed**
  for the current source set. Next useful step is likely a fresh re-scan for *new* dated outputs on
  already-populated flagships, or progressing the gated tier.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 08:07 UTC heartbeat — AISI publications date-refine (pubs=0 gap closed → pivot to null-date quality)

With the `pubs=0` flagship backfill gap closed (07:37), shifted to the next-best useful work:
a **data-quality re-scan** of already-populated flagships. Found **AISI** carrying 4
`publications[]` entries with `date: null` (logged that way in the 2026-06-26 backfill, flagged
"refine on a later pass"). Null-dated pubs are silently **dropped by synth-feed** (it only emits
items with a real date) — so these 4 were dashboard-invisible despite being present in the feed file.

### Per-page byline date verification (HTTP 200 each)
Fetched all 4 AISI blog pages and took the date from each page's own rendered **"— <author> — <date>"
lede byline** (the site is JS-rendered with no JSON-LD `datePublished`/ISO date in the payload, so the
visible byline is the primary source):
- Research Agenda → **2025-05-06**
- Our evaluation of OpenAI's GPT-5.5 cyber capabilities → **2026-04-30** ("— Apr 30, 2026")
- Conference on frontier AI safety frameworks → **2024-09-19** ("— Sep 19, 2024"; note the conference
  *event* was Nov 20-21 2024 — used the post's publish byline, not the event date)
- Safety cases at AISI → **2024-08-23** ("Geoffrey Irving — Aug 23, 2024")

**Verification catch:** every page also contains a shared footer note *"we changed our name to the AI
Security Institute on 14 February 2025"*. A naive date-grab would have mis-dated two pages to
**2025-02-14**. Distinguished the real `— <author> —` byline from this name-change footer.

aisi `publications[]` nulls **4→0**; synth-feed dry-run→write added exactly **4** events;
feed.json **217→221**. Both JSON re-validated. Commit e323f97.

## Carried forward (updated)
- Publications: `pubs=0` flagship gap closed; AISI null-date gap now also closed. Next useful
  pass: re-scan other already-populated flagships for **remaining null-dated** publications (same
  refine discipline), or fresh **new dated outputs** on high-frequency labs (DeepMind latest 2026-06-24,
  Stability latest 2026-06-03). Then progress the gated tier.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 08:37 UTC heartbeat — OII publications date-refine (Crossref-verified, 3 null→dated)

Continued the null-date re-scan pass. A scan across all `feeds/*.json publications[]` surfaced a
sizable remaining null-date backlog (ada-lovelace 4, alan-turing 3, ico-ai 2, lse 2, public-io 3,
oxford-internet-institute 3, demos 2, cltr 2, + several 1s). Picked **oxford-internet-institute**
this pass because all 3 nulls are **DOI** URLs — the gold-standard date source via the Crossref API
(authoritative `published-online`), no JS-rendered-byline guesswork needed.

### Crossref `published-online` dates (api.crossref.org/works/<doi>)
- Training language models to be warm... (10.1038/s41586-026-10410-0) → **2026-04-29** (print 04-30)
- Reliability of LLMs as medical assistants... (10.1038/s41591-025-04074-y) → **2026-02-09**
  (Crossref `published` only carried month 2026-02; used the full `published-online` 02-09)
- A Framework for Evaluating Global AI Governance Initiatives (10.1111/1758-5899.70164) → **2026-04-07**

oxford-internet-institute `publications[]` nulls **3→0**; synth-feed added exactly **3** events;
feed.json **221→224**. Both JSON re-validated.

## 09:07 UTC heartbeat — LSE publications date-refine (Crossref-verified, 2 null→dated)

Worked the queued **lse-ai-policy** target (both nulls were DOI URLs → Crossref `published-online`,
the authoritative gold-standard dating, no JS-byline guesswork).

### Crossref `published-online` dates (api.crossref.org/works/<doi>)
- Omission/hallucination prevalence of clinical guidelines in diagnostic LLM outputs
  (10.1136/bmjhci-2025-101959, BMJ Health & Care Informatics) → **2026-04-24**
  (Crossref `published`/`published-print` only carried month 2026-04; used the full
  `published-online` 04-24, consistent with the OII Nature-pair refine discipline).
- AI, IP & human rights: mapping the legal landscape in European health systems
  (10.1038/s44401-025-00050-3, npj Health Systems) → **2025-11-25** (published-online == issued).

Titles cross-confirmed against the feed entries (container journals match). lse-ai-policy
`publications[]` nulls **2→0**; synth-feed dry-run→write added exactly **2** events;
feed.json **224→226**. Both JSON re-validated; both events confirmed present
(company_slug=lse-ai-policy, event_type=publication, summaries intact).

## Carried forward (updated)
- Null-date publications backlog (next refine targets, by ease of verification):
  - ~~lse-ai-policy (2)~~ DONE → DOI/Crossref tier now exhausted among the easy wins.
  - **ada-lovelace** (4), **alan-turing** (3), **public-io** (3), **demos** (2), **cltr** (2),
    **ico-ai** (2) — first-party HTML byline/JSON-LD dating (check for DOIs first; HTML-byline rest).
  - Singles: benevolentai, cetas, cohere-london, faculty-ai.
- Gated tier (~43 law firms/consultancies/regulators) — still needs Techie's browser-fetch path.

## 10:07 UTC heartbeat — Ada Lovelace publications date-refine (3 null→dated, citation-meta-verified)

Continued the null-date re-scan (after public-io at 09:36). Picked **ada-lovelace-institute**
(largest remaining null backlog: 4). The 4 nulls are all first-party adalovelaceinstitute.org
report/policy-briefing pages — no DOIs, so per-page HTML date verification (no Crossref shortcut).

### Authoritative date source found: Highwire/Scholar `citation_publication_date` meta
These report pages carry `<meta name="citation_publication_date" content="YYYY/MM/DD">` (Google
Scholar citation metadata) — the canonical publish date, each cross-confirmed against the page's
visible 'DD Month YYYY' date block (HTTP 200 each):
- Measuring up: evaluating claims about AI and productivity in the UK public sector → **2026-05-11**
  (citation_publication_date 2026/05/11 == visible '11 May 2026').
- Navigating the future: AI in career guidance for young people → **2026-04-13** (== '13 April 2026').
- Scribe and prejudice? AI transcription tools in social care → **2026-02-11** (== '11 February 2026').

### Honest no-op on the 4th (verification-bar discipline)
- 'I love it, but I hate it' (.../report/i-love-it-but-i-hate-it/) **301-redirects to the Nuffield
  Foundation** canonical (co-published report). The Nuffield page exposes **no**
  `citation_publication_date` and no clean visible publish-date block — only an
  `article:modified_time` 2026-05-21 (a *modified* time, NOT a publish date). Left it **null** rather
  than pad with a modified-time or a body 'accessed 24 March 2026' citation date. Backfill on a later
  pass if a first-party publish date surfaces.

ada-lovelace `publications[]` nulls **4→3** (1 left, deliberately). synth-feed dry-run→write added
exactly **3** events; feed.json **228→231**. Both JSON re-validated; all 3 events confirmed present
(company_slug=ada-lovelace-institute, event_type=publication, canonical report URLs).

## Carried forward (updated)
- Null-date publications backlog (next refine targets): **alan-turing** (3), **demos** (2),
  **cltr** (2), **ico-ai** (2), then singles (benevolentai, cetas, cohere-london, faculty-ai,
  public-io 1). Check DOIs first (Crossref), else first-party HTML byline / citation_publication_date.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators) — still needs Techie's browser-fetch path.

## 10:37 UTC heartbeat — demos date-refine (2 null→dated, JSON-LD+meta dual-verified) + alan-turing deferred to gated tier

### alan-turing attempted first, deferred (verification-bar discipline)
Next-by-queue was alan-turing (3 nulls, all turing.ac.uk /news/ pages, no DOIs). **turing.ac.uk is
Cloudflare-gated** ("Just a moment..." interstitial) — curl 403 and web_fetch 403 on all 3. Brave
surfaced a `published: April 30, 2026` for the frontier-AI page, but a **search-engine published
field is NOT a first-party publish date** (crawl/estimate; fails the bar). Did **not** write from it.
→ alan-turing (3) moved to the **gated tier** (needs Techie's browser-fetch path), same as the ~43
law-firm/consultancy set. Picked the next reachable target instead.

### demos (2 null→dated) — first-party WordPress JSON-LD datePublished == article:published_time
Both demos.co.uk/research/ pages HTTP 200, each carries dual first-party signals (JSON-LD
`datePublished` identical to `<meta property="article:published_time">`):
- Citizens Economy: a New Deal to power economic growth… → **2026-06-26**
  (datePublished 2026-06-26T05:00:53Z == published_time). Note: dateModified 2026-06-25 is *earlier*
  than published (CMS quirk) — used the canonical published_time, not modified.
- Fairer Feeds: public interest news prominence rules… → **2026-06-12**
  (datePublished 2026-06-12T08:47:56Z == published_time).
No plain-text visible date block (rendered non-matchably), but the JSON-LD+meta dual match is the
same authoritative tier used for public-io/ada-lovelace. Cleared the bar.

demos `publications[]` nulls **2→0**. synth-feed dry-run→write added exactly **2** events;
feed.json **231→233**. Both JSON re-validated; both events confirmed present (company_slug=demos,
event_type=publication, canonical demos.co.uk/research URLs).

## Carried forward (updated)
- Null-date publications backlog (next refine targets): **cltr** (2), **ico-ai** (2), then singles
  (benevolentai, cetas, cohere-london, faculty-ai, public-io 1). Check reachability + DOIs first,
  else first-party HTML byline / JSON-LD datePublished / citation_publication_date.
- **alan-turing (3)** → gated tier (Cloudflare-blocked), needs Techie's browser-fetch path.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators + now alan-turing) — needs browser-fetch path.

## 11:37 UTC heartbeat — ico-ai date-refine ATTEMPTED → honest no-op (DC.Date is a CMS-refresh trap, not a publish date)

Worked the queued **ico-ai** target (2 null-dated publications). Both are ico.org.uk
/our-work-on-artificial-intelligence/ pages (HTTP 200, no DOIs). The only date metadata exposed
is a Dublin Core `<meta name="DC.Date">` field — and a cross-page test shows it is **not a
trustworthy publish date** (same CMS-republish trap as the CLTR-SDR / demos-dateModified
exclusions). Did **not** write from it.

### The DC.Date trap (why I excluded it)
- Page 1 "response to the consultation series on generative AI" → DC.Date **Friday, May 22, 2026**.
  But this is the documented **13 December 2024** consultation-series *outcomes/response* — three
  independent legal trackers agree: Skadden ("On 13 December 2024… published the report of
  outcomes"), ai-regulation.com (Dec 2024), Osborne Clarke ("December 2024"). An 18-month gap
  between the real publication and DC.Date = a CMS landing-page refresh date, NOT publish.
- Page 2 "Generative AI fourth call for evidence" → DC.Date **Monday, September 08, 2025**, yet the
  page's **visible body carries no 2025 date at all** (year counts: 2024×2, 2020, 2019). DC.Date
  again contradicts the body.
- Control: the ICO **GenAI hub** page (/GenAI) → DC.Date **Monday, September 01, 2025** — the hub has
  existed since the series launched in 2024, so a Sep-2025 DC.Date there confirms the field is a
  site-wide template/refresh stamp, not a per-document publish date.
- Weekday-consistency passes (May 22 2026=Fri, Sep 8 2025=Mon) but that only proves the strings are
  well-formed, not that they're publish dates. No JSON-LD `datePublished` / `article:published_time`
  on either page; no clean visible "published DD Month YYYY" block (JS-rendered).

### Decision
Honest **no-op** — left both `date: null` rather than pad with an untrustworthy DC.Date or a
third-party-tracker date (below the first-party bar set in prior commits). This is a useful negative
result: it documents that **ico.org.uk DC.Date must NOT be used as a publish date** for any future
pass on this source.
- **Lead (not written):** Page 1's true publication date is almost certainly **2024-12-13**
  (strong 3-way third-party corroboration of the outcomes report). If a future first-party pass
  confirms 13 Dec 2024 from ICO's own rendered page, page 1 can be dated then.
→ ico-ai (2) moved to the **gated/first-party-verification tier** (like alan-turing), needs a
  browser-fetch path that can surface a real per-document publish date.

## Carried forward (updated)
- Null-date publications backlog (next refine targets): singles — **benevolentai, cetas,
  cohere-london, faculty-ai, public-io (1)**. Check reachability + DOIs first, else first-party
  HTML byline / JSON-LD datePublished / citation_publication_date. AVOID CMS DC.Date/dateModified.
- **alan-turing (3)** + **ico-ai (2)** → gated tier (Cloudflare / DC.Date-only), need browser-fetch.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators + alan-turing + ico-ai) — needs browser-fetch path.

---

## 12:10 — singles date-refine (3 filled, 2 honest no-ops → gated)

Worked the singles null-date backlog (benevolentai, cohere-london, faculty-ai, cetas, public-io).
All five URLs HTTP 200. Filled **3** via first-party metadata; **2** honest no-ops to gated tier.

**Filled:**
- **benevolentai** "Artificial intelligence in drug discovery" → **2018-08-13**. This is a commentary
  published in *Future Medicinal Chemistry* (DOI `10.4155/fmc-2018-0212`). Used the authoritative
  **Crossref** record: `issued`/`published-online` = 2018-08-13, title exact-match. Scholarly DOI
  date — strongest first-party provenance, better than any CMS field.
- **cohere-london** "Why Cultural Awareness is Essential for Global AI" → **2026-06-23**. JSON-LD
  `datePublished` 2026-06-23 **==** visible body "Jun 23, 2026" **==** dateModified (clean, no CMS skew).
- **faculty-ai** "Beyond manual red teaming…" → **2026-06-26**. JSON-LD `datePublished` 2026-06-26,
  no conflicting dateModified.

**Honest no-ops (→ gated/browser-fetch tier):**
- **cetas** "Asia-Pacific AI Governance…" — only metadata was `citation_publication_date` = epoch
  **1652349769 → 2022-05-12**, and the stored URL is the *listing* page (`/research-and-publications`),
  not the article. Wrong/untrustworthy for this specific publication. Left null.
- **public-io** "Phase One Complete… Thames Freeport" — no JSON-LD `datePublished` in static HTML;
  the visible "Jun 03 2026" is JS-injected and not confirmable from a static first-party byline.
  Left null rather than pad below the first-party bar.

synth-feed: 235 → **237** events. feed.json re-validated; all 3 new events confirmed present
by url-slug lookup. cetas + public-io join the gated/first-party-verification tier (need a
browser-fetch path that surfaces a real per-document publish date).

## Carried forward (updated)
- Singles backlog now CLEARED except gated: **cetas (1)**, **public-io (1)** → gated tier.
- **alan-turing (3)** + **ico-ai (2)** → gated tier (Cloudflare / DC.Date-only).
- Ada Lovelace: 1 deliberate null (Nuffield-co-published, no first-party date).
- Gated tier (~43 firms/consultancies/regulators + the above) — needs browser-fetch path. This is
  now the dominant remaining lever; next non-gated refine work is largely exhausted.

## 12:40 — ODI news[] date-refine (2 filled → dashboard-visible)

Found 2 null-dated items I'd previously overlooked: they live in **odi.json `news[]`**, not
`publications[]`, which is why the recent singles-backlog sweeps missed them. Confirmed `news[]`
**is** ingested by synth-feed (as `press` events) — the stale "only publications[] ingested" note
no longer holds. So dating these makes them dashboard-visible, not just tidy.

Both `theodi.org` URLs HTTP 200. No JSON-LD `datePublished` / `article:published_time` /
`dateModified` / DC.Date in static HTML — but each page carries exactly **one** first-party
`<time datetime>` element with matching human-readable text:
- "New research questions the trustworthiness of AI chatbots for government information"
  → **2026-02-05** (`<time datetime="2026-02-05">Thu Feb 5, 2026</time>`)
- "ODI and SAP announce partnership… trustworthy AI-ready data infrastructure"
  → **2026-03-24** (`<time datetime="2026-03-24">Tue Mar 24, 2026</time>`)
Single `<time>` per page = the article publish stamp, no listing ambiguity; datetime attr == visible
text. First-party bar met.

synth-feed (dry-run then write): 237 existing preserved, **2 new `press` events added (both odi)**,
0 conflicts. **feed.json 237 → 239.** odi.json news[] nulls 2→0. Both JSON re-validated; both
events confirmed present.

## Carried forward (updated)
- Non-gated null-date backlog now genuinely exhausted across publications[] AND news[].
- Gated tier still dominant: cetas (1), public-io (1), alan-turing (3), ico-ai (2),
  ~43 firms/consultancies/regulators — all need a browser-fetch path.
- Ada Lovelace: 1 deliberate null (Nuffield-co-published, no first-party date).

---

# Glyphie notes — 2026-06-28

## 01:08 UTC heartbeat (quiet hours) — publications-backfill target triage

Continued the carried-forward **publications gap** work (`pubs=0` flagship feeds still
dashboard-invisible: helsing, synthesia, meta-london, elevenlabs).

### ElevenLabs — ruled OUT as a publications-backfill target (useful negative result)
- ElevenLabs is a **product company, not a research-publishing lab**. Its blog "Research"
  category = model/product announcements (e.g. *Introducing Dubbing v2* listed May 28 2026,
  *Introducing Music v2* listed May 26 2026), NOT academic papers.
- Could **not** meet the per-page date-verification discipline used for Stability AI / Apollo:
  the blog post pages are JS-rendered and return a soft **404** to curl (even with a browser UA),
  exposing **no JSON-LD `datePublished`** or `article:published_time` to verify against. The only
  dates available are the blog **listing-card** dates — which we treat as UNTRUSTED leads, not
  primary `date` values.
- arXiv search surfaced only **third-party** deepfake-detection papers that *use* ElevenLabs
  audio as a dataset (e.g. 2307.07683, 2512.09221) — these are NOT ElevenLabs publications.
- **Decision:** do NOT backfill ElevenLabs `publications[]`. Adding listing-card-dated product
  posts would drop below the evidentiary bar set in prior commits. Left `publications: []` intact;
  no repo change this heartbeat (honest no-op rather than a low-quality fill).

### Synthesia — identified as the better next target (verifiable academic research)
- synthesia.io/research (status 200) surfaces genuine, citable outputs:
  - **HumanRF** — temporally-stable novel-view synthesis of humans in motion (paper).
  - **ActorsHQ** — high-fidelity 160-camera multi-view human-motion dataset.
  - Foundational research with co-founder **Prof. Matthias Nießner**; both have arXiv presence.
- These can be verified against arXiv `datePublished` / abs pages — the standard I require.
- **Next heartbeat:** do the Synthesia publications backfill — pull each paper's real arXiv id +
  submission date (treat the /research index as an untrusted lead), add 2–4 dated `publications[]`
  entries, re-run synth-feed (dry-run then write), re-validate both JSON files. Then re-assess
  helsing / meta-london.

## 01:37 UTC heartbeat (quiet hours) — Synthesia publications backfill DONE

Executed the queued Synthesia backfill with full arXiv per-page verification:
- **HumanRF** verified on arXiv abs page: **2305.06356**, submitted **2023-05-10**, authors incl.
  Synthesia Chief Scientist **Jonathan Starck** + Prof. **Matthias Nießner** + Prof. **Lourdes Agapito**.
- **Correction vs prior note:** ActorsHQ is **NOT a separate paper** — the abstract of 2305.06356
  explicitly *introduces* ActorsHQ as the 160-camera/12MP dataset *within* the HumanRF paper.
  Treating them as two outputs would have double-counted. Logged as **one** publication entry
  (title flags the ActorsHQ dataset contribution).
- Confirmed no other arXiv-verifiable Synthesia output: au:Starck_Jonathan returns only HumanRF.
  Resisted padding with the /research listing's untrusted secondary links.
- Canonical `url` = arXiv abs page (primary, date-verifiable), NOT the synthesia.io/research card.
- synthesia `publications[]` 0→1; synth-feed dry-run→write added exactly 1 event; feed.json 209→210.
  Both JSON files re-validated; event confirmed present with company_slug=synthesia, type=publication.

## Carried forward (updated)
- Publications backfill queue (verifiable-first): ~~synthesia~~ DONE → **helsing (next)**, then meta-london.
- ElevenLabs: publications-backfill target REMOVED (no first-party verifiable research). Its rich
  BIPA/Vacker controversy stays the main signal for that feed.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 07:10 UTC heartbeat — CMA-DMU publications backfill DONE (queue re-pointed off ruled-out targets)

The carried-forward "helsing (next)" queue pointer was stale: helsing was already
triaged **OUT** (commit beff1c4 — defence-AI, no first-party verifiable research; the AAAI
award is a pre-Helsing misattribution). Re-scan of remaining `pubs=0` feeds:
- Among feeds carrying real role/leadership signal, only **elevenlabs** + **helsing** remained
  at pubs=0 — both already ruled out. That tier is **exhausted** (padding would breach the bar).
- Full `pubs=0` set: cma-dmu, elevenlabs, helsing, improbable, nhs-ai-transformation.
  improbable = metaverse/Web3 product co (no first-party academic output). That leaves the two
  genuine **policy-body publishers**: CMA-DMU and NHS-AI-transformation.

### CMA-DMU — backfilled with its flagship AI Foundation Models programme (verifiable)
Added 2 first-party CMA policy reports, both primary gov.uk date-verified per-page:
- **Initial report** — 2023-09-18 (`govuk:first-published-at`/JSON-LD datePublished, HTTP 200,
  `/ai-foundation-models-initial-report`).
- **Update paper** — 2024-04-11 (same primary verification, `/ai-foundation-models-update-paper`).
Titles taken from each page's og:title. type=policy_report, byline=Competition and Markets Authority.
Left out the Sept-2023 "technical update report" — no per-page date-verified canonical gov.uk URL yet
(no padding from search-snippet dates).

cma-dmu `publications[]` 0→2; synth-feed dry-run→write added exactly 2 events; feed.json 213→215.
Both JSON files re-validated; both events confirmed (company_slug=cma-dmu, event_type=publication).

## Carried forward (updated)
- Publications backfill queue: verifiable-company tier **EXHAUSTED** (elevenlabs/helsing/improbable
  all honest no-ops). Remaining genuine target: **NHS-AI-transformation** — check NHS England /
  NICE / Transformation Directorate for dated first-party policy/guidance publications (next).
- ElevenLabs/Helsing/Improbable: publications-backfill targets REMOVED (no first-party verifiable research).
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 07:37 UTC heartbeat — NHS-AI-transformation publications backfill DONE

Worked the queued **NHS-AI-transformation** target (the last genuine policy-body `pubs=0`
flagship after CMA-DMU at 07:09). Added 2 first-party NHS England / Transformation Directorate
AI **guidance** documents, each date-verified from the page's own rendered text (not search snippets):
- **AI-enabled ambient scribing guidance** — page states *"published: 27 April, 2025"*,
  Publication reference **PRN01734_i**, HTTP 200. IG/clinical-safety guidance for adopting AVT
  ambient-scribing products (DCB0160/DPIA, supplier/AVT register).
- **AI & machine learning (GP electronic records good-practice guidelines)** — page states
  *"Version 1.2 28 March 2025"*, HTTP 200.

Both are **distinct** from the two NHS England press items already in `news[]` (2026-06-08,
2024-12-12). type=`policy_report` (guidance/policy output, not academic research — consistent with
how CMA-DMU/DSIT policy bodies are logged). Left out third-party / ICB-level AI policies (e.g.
Cheshire-Merseyside sub-strategy, MIAA checklist, InnovateHealth provider template) — those are
**not** NHS-England-authored, so they'd breach the first-party bar.

nhs-ai-transformation `publications[]` 0→2; synth-feed dry-run→write added exactly 2 events;
feed.json 215→217. Both JSON files re-validated; events confirmed present
(company_slug=nhs-ai-transformation, event_type=publication).

## Carried forward (updated)
- Publications backfill queue: **all genuine policy-body + verifiable-company targets now done or
  ruled out** — DSIT (2), CMA-DMU (2), NHS (2) backfilled; elevenlabs/helsing/improbable honest
  no-ops (no first-party verifiable research). The `pubs=0` flagship gap is effectively **closed**
  for the current source set. Next useful step is likely a fresh re-scan for *new* dated outputs on
  already-populated flagships, or progressing the gated tier.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 08:07 UTC heartbeat — AISI publications date-refine (pubs=0 gap closed → pivot to null-date quality)

With the `pubs=0` flagship backfill gap closed (07:37), shifted to the next-best useful work:
a **data-quality re-scan** of already-populated flagships. Found **AISI** carrying 4
`publications[]` entries with `date: null` (logged that way in the 2026-06-26 backfill, flagged
"refine on a later pass"). Null-dated pubs are silently **dropped by synth-feed** (it only emits
items with a real date) — so these 4 were dashboard-invisible despite being present in the feed file.

### Per-page byline date verification (HTTP 200 each)
Fetched all 4 AISI blog pages and took the date from each page's own rendered **"— <author> — <date>"
lede byline** (the site is JS-rendered with no JSON-LD `datePublished`/ISO date in the payload, so the
visible byline is the primary source):
- Research Agenda → **2025-05-06**
- Our evaluation of OpenAI's GPT-5.5 cyber capabilities → **2026-04-30** ("— Apr 30, 2026")
- Conference on frontier AI safety frameworks → **2024-09-19** ("— Sep 19, 2024"; note the conference
  *event* was Nov 20-21 2024 — used the post's publish byline, not the event date)
- Safety cases at AISI → **2024-08-23** ("Geoffrey Irving — Aug 23, 2024")

**Verification catch:** every page also contains a shared footer note *"we changed our name to the AI
Security Institute on 14 February 2025"*. A naive date-grab would have mis-dated two pages to
**2025-02-14**. Distinguished the real `— <author> —` byline from this name-change footer.

aisi `publications[]` nulls **4→0**; synth-feed dry-run→write added exactly **4** events;
feed.json **217→221**. Both JSON re-validated. Commit e323f97.

## Carried forward (updated)
- Publications: `pubs=0` flagship gap closed; AISI null-date gap now also closed. Next useful
  pass: re-scan other already-populated flagships for **remaining null-dated** publications (same
  refine discipline), or fresh **new dated outputs** on high-frequency labs (DeepMind latest 2026-06-24,
  Stability latest 2026-06-03). Then progress the gated tier.
- Gated tier (~43 law firms/consultancies/regulators) — still need Techie's browser-fetch path.

## 08:37 UTC heartbeat — OII publications date-refine (Crossref-verified, 3 null→dated)

Continued the null-date re-scan pass. A scan across all `feeds/*.json publications[]` surfaced a
sizable remaining null-date backlog (ada-lovelace 4, alan-turing 3, ico-ai 2, lse 2, public-io 3,
oxford-internet-institute 3, demos 2, cltr 2, + several 1s). Picked **oxford-internet-institute**
this pass because all 3 nulls are **DOI** URLs — the gold-standard date source via the Crossref API
(authoritative `published-online`), no JS-rendered-byline guesswork needed.

### Crossref `published-online` dates (api.crossref.org/works/<doi>)
- Training language models to be warm... (10.1038/s41586-026-10410-0) → **2026-04-29** (print 04-30)
- Reliability of LLMs as medical assistants... (10.1038/s41591-025-04074-y) → **2026-02-09**
  (Crossref `published` only carried month 2026-02; used the full `published-online` 02-09)
- A Framework for Evaluating Global AI Governance Initiatives (10.1111/1758-5899.70164) → **2026-04-07**

oxford-internet-institute `publications[]` nulls **3→0**; synth-feed added exactly **3** events;
feed.json **221→224**. Both JSON re-validated.

## 09:07 UTC heartbeat — LSE publications date-refine (Crossref-verified, 2 null→dated)

Worked the queued **lse-ai-policy** target (both nulls were DOI URLs → Crossref `published-online`,
the authoritative gold-standard dating, no JS-byline guesswork).

### Crossref `published-online` dates (api.crossref.org/works/<doi>)
- Omission/hallucination prevalence of clinical guidelines in diagnostic LLM outputs
  (10.1136/bmjhci-2025-101959, BMJ Health & Care Informatics) → **2026-04-24**
  (Crossref `published`/`published-print` only carried month 2026-04; used the full
  `published-online` 04-24, consistent with the OII Nature-pair refine discipline).
- AI, IP & human rights: mapping the legal landscape in European health systems
  (10.1038/s44401-025-00050-3, npj Health Systems) → **2025-11-25** (published-online == issued).

Titles cross-confirmed against the feed entries (container journals match). lse-ai-policy
`publications[]` nulls **2→0**; synth-feed dry-run→write added exactly **2** events;
feed.json **224→226**. Both JSON re-validated; both events confirmed present
(company_slug=lse-ai-policy, event_type=publication, summaries intact).

## Carried forward (updated)
- Null-date publications backlog (next refine targets, by ease of verification):
  - ~~lse-ai-policy (2)~~ DONE → DOI/Crossref tier now exhausted among the easy wins.
  - **ada-lovelace** (4), **alan-turing** (3), **public-io** (3), **demos** (2), **cltr** (2),
    **ico-ai** (2) — first-party HTML byline/JSON-LD dating (check for DOIs first; HTML-byline rest).
  - Singles: benevolentai, cetas, cohere-london, faculty-ai.
- Gated tier (~43 law firms/consultancies/regulators) — still needs Techie's browser-fetch path.

## 10:07 UTC heartbeat — Ada Lovelace publications date-refine (3 null→dated, citation-meta-verified)

Continued the null-date re-scan (after public-io at 09:36). Picked **ada-lovelace-institute**
(largest remaining null backlog: 4). The 4 nulls are all first-party adalovelaceinstitute.org
report/policy-briefing pages — no DOIs, so per-page HTML date verification (no Crossref shortcut).

### Authoritative date source found: Highwire/Scholar `citation_publication_date` meta
These report pages carry `<meta name="citation_publication_date" content="YYYY/MM/DD">` (Google
Scholar citation metadata) — the canonical publish date, each cross-confirmed against the page's
visible 'DD Month YYYY' date block (HTTP 200 each):
- Measuring up: evaluating claims about AI and productivity in the UK public sector → **2026-05-11**
  (citation_publication_date 2026/05/11 == visible '11 May 2026').
- Navigating the future: AI in career guidance for young people → **2026-04-13** (== '13 April 2026').
- Scribe and prejudice? AI transcription tools in social care → **2026-02-11** (== '11 February 2026').

### Honest no-op on the 4th (verification-bar discipline)
- 'I love it, but I hate it' (.../report/i-love-it-but-i-hate-it/) **301-redirects to the Nuffield
  Foundation** canonical (co-published report). The Nuffield page exposes **no**
  `citation_publication_date` and no clean visible publish-date block — only an
  `article:modified_time` 2026-05-21 (a *modified* time, NOT a publish date). Left it **null** rather
  than pad with a modified-time or a body 'accessed 24 March 2026' citation date. Backfill on a later
  pass if a first-party publish date surfaces.

ada-lovelace `publications[]` nulls **4→3** (1 left, deliberately). synth-feed dry-run→write added
exactly **3** events; feed.json **228→231**. Both JSON re-validated; all 3 events confirmed present
(company_slug=ada-lovelace-institute, event_type=publication, canonical report URLs).

## Carried forward (updated)
- Null-date publications backlog (next refine targets): **alan-turing** (3), **demos** (2),
  **cltr** (2), **ico-ai** (2), then singles (benevolentai, cetas, cohere-london, faculty-ai,
  public-io 1). Check DOIs first (Crossref), else first-party HTML byline / citation_publication_date.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators) — still needs Techie's browser-fetch path.

## 10:37 UTC heartbeat — demos date-refine (2 null→dated, JSON-LD+meta dual-verified) + alan-turing deferred to gated tier

### alan-turing attempted first, deferred (verification-bar discipline)
Next-by-queue was alan-turing (3 nulls, all turing.ac.uk /news/ pages, no DOIs). **turing.ac.uk is
Cloudflare-gated** ("Just a moment..." interstitial) — curl 403 and web_fetch 403 on all 3. Brave
surfaced a `published: April 30, 2026` for the frontier-AI page, but a **search-engine published
field is NOT a first-party publish date** (crawl/estimate; fails the bar). Did **not** write from it.
→ alan-turing (3) moved to the **gated tier** (needs Techie's browser-fetch path), same as the ~43
law-firm/consultancy set. Picked the next reachable target instead.

### demos (2 null→dated) — first-party WordPress JSON-LD datePublished == article:published_time
Both demos.co.uk/research/ pages HTTP 200, each carries dual first-party signals (JSON-LD
`datePublished` identical to `<meta property="article:published_time">`):
- Citizens Economy: a New Deal to power economic growth… → **2026-06-26**
  (datePublished 2026-06-26T05:00:53Z == published_time). Note: dateModified 2026-06-25 is *earlier*
  than published (CMS quirk) — used the canonical published_time, not modified.
- Fairer Feeds: public interest news prominence rules… → **2026-06-12**
  (datePublished 2026-06-12T08:47:56Z == published_time).
No plain-text visible date block (rendered non-matchably), but the JSON-LD+meta dual match is the
same authoritative tier used for public-io/ada-lovelace. Cleared the bar.

demos `publications[]` nulls **2→0**. synth-feed dry-run→write added exactly **2** events;
feed.json **231→233**. Both JSON re-validated; both events confirmed present (company_slug=demos,
event_type=publication, canonical demos.co.uk/research URLs).

## Carried forward (updated)
- Null-date publications backlog (next refine targets): **cltr** (2), **ico-ai** (2), then singles
  (benevolentai, cetas, cohere-london, faculty-ai, public-io 1). Check reachability + DOIs first,
  else first-party HTML byline / JSON-LD datePublished / citation_publication_date.
- **alan-turing (3)** → gated tier (Cloudflare-blocked), needs Techie's browser-fetch path.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators + now alan-turing) — needs browser-fetch path.

## 11:37 UTC heartbeat — ico-ai date-refine ATTEMPTED → honest no-op (DC.Date is a CMS-refresh trap, not a publish date)

Worked the queued **ico-ai** target (2 null-dated publications). Both are ico.org.uk
/our-work-on-artificial-intelligence/ pages (HTTP 200, no DOIs). The only date metadata exposed
is a Dublin Core `<meta name="DC.Date">` field — and a cross-page test shows it is **not a
trustworthy publish date** (same CMS-republish trap as the CLTR-SDR / demos-dateModified
exclusions). Did **not** write from it.

### The DC.Date trap (why I excluded it)
- Page 1 "response to the consultation series on generative AI" → DC.Date **Friday, May 22, 2026**.
  But this is the documented **13 December 2024** consultation-series *outcomes/response* — three
  independent legal trackers agree: Skadden ("On 13 December 2024… published the report of
  outcomes"), ai-regulation.com (Dec 2024), Osborne Clarke ("December 2024"). An 18-month gap
  between the real publication and DC.Date = a CMS landing-page refresh date, NOT publish.
- Page 2 "Generative AI fourth call for evidence" → DC.Date **Monday, September 08, 2025**, yet the
  page's **visible body carries no 2025 date at all** (year counts: 2024×2, 2020, 2019). DC.Date
  again contradicts the body.
- Control: the ICO **GenAI hub** page (/GenAI) → DC.Date **Monday, September 01, 2025** — the hub has
  existed since the series launched in 2024, so a Sep-2025 DC.Date there confirms the field is a
  site-wide template/refresh stamp, not a per-document publish date.
- Weekday-consistency passes (May 22 2026=Fri, Sep 8 2025=Mon) but that only proves the strings are
  well-formed, not that they're publish dates. No JSON-LD `datePublished` / `article:published_time`
  on either page; no clean visible "published DD Month YYYY" block (JS-rendered).

### Decision
Honest **no-op** — left both `date: null` rather than pad with an untrustworthy DC.Date or a
third-party-tracker date (below the first-party bar set in prior commits). This is a useful negative
result: it documents that **ico.org.uk DC.Date must NOT be used as a publish date** for any future
pass on this source.
- **Lead (not written):** Page 1's true publication date is almost certainly **2024-12-13**
  (strong 3-way third-party corroboration of the outcomes report). If a future first-party pass
  confirms 13 Dec 2024 from ICO's own rendered page, page 1 can be dated then.
→ ico-ai (2) moved to the **gated/first-party-verification tier** (like alan-turing), needs a
  browser-fetch path that can surface a real per-document publish date.

## Carried forward (updated)
- Null-date publications backlog (next refine targets): singles — **benevolentai, cetas,
  cohere-london, faculty-ai, public-io (1)**. Check reachability + DOIs first, else first-party
  HTML byline / JSON-LD datePublished / citation_publication_date. AVOID CMS DC.Date/dateModified.
- **alan-turing (3)** + **ico-ai (2)** → gated tier (Cloudflare / DC.Date-only), need browser-fetch.
- Ada Lovelace: 1 deliberate null left (Nuffield-co-published i-love-it report — no first-party date).
- Gated tier (~43 law firms/consultancies/regulators + alan-turing + ico-ai) — needs browser-fetch path.

---

## 12:10 — singles date-refine (3 filled, 2 honest no-ops → gated)

Worked the singles null-date backlog (benevolentai, cohere-london, faculty-ai, cetas, public-io).
All five URLs HTTP 200. Filled **3** via first-party metadata; **2** honest no-ops to gated tier.

**Filled:**
- **benevolentai** "Artificial intelligence in drug discovery" → **2018-08-13**. This is a commentary
  published in *Future Medicinal Chemistry* (DOI `10.4155/fmc-2018-0212`). Used the authoritative
  **Crossref** record: `issued`/`published-online` = 2018-08-13, title exact-match. Scholarly DOI
  date — strongest first-party provenance, better than any CMS field.
- **cohere-london** "Why Cultural Awareness is Essential for Global AI" → **2026-06-23**. JSON-LD
  `datePublished` 2026-06-23 **==** visible body "Jun 23, 2026" **==** dateModified (clean, no CMS skew).
- **faculty-ai** "Beyond manual red teaming…" → **2026-06-26**. JSON-LD `datePublished` 2026-06-26,
  no conflicting dateModified.

**Honest no-ops (→ gated/browser-fetch tier):**
- **cetas** "Asia-Pacific AI Governance…" — only metadata was `citation_publication_date` = epoch
  **1652349769 → 2022-05-12**, and the stored URL is the *listing* page (`/research-and-publications`),
  not the article. Wrong/untrustworthy for this specific publication. Left null.
- **public-io** "Phase One Complete… Thames Freeport" — no JSON-LD `datePublished` in static HTML;
  the visible "Jun 03 2026" is JS-injected and not confirmable from a static first-party byline.
  Left null rather than pad below the first-party bar.

synth-feed: 235 → **237** events. feed.json re-validated; all 3 new events confirmed present
by url-slug lookup. cetas + public-io join the gated/first-party-verification tier (need a
browser-fetch path that surfaces a real per-document publish date).

## Carried forward (updated)
- Singles backlog now CLEARED except gated: **cetas (1)**, **public-io (1)** → gated tier.
- **alan-turing (3)** + **ico-ai (2)** → gated tier (Cloudflare / DC.Date-only).
- Ada Lovelace: 1 deliberate null (Nuffield-co-published, no first-party date).
- Gated tier (~43 firms/consultancies/regulators + the above) — needs browser-fetch path. This is
  now the dominant remaining lever; next non-gated refine work is largely exhausted.

## 12:40 — ODI news[] date-refine (2 filled → dashboard-visible)

Found 2 null-dated items I'd previously overlooked: they live in **odi.json `news[]`**, not
`publications[]`, which is why the recent singles-backlog sweeps missed them. Confirmed `news[]`
**is** ingested by synth-feed (as `press` events) — the stale "only publications[] ingested" note
no longer holds. So dating these makes them dashboard-visible, not just tidy.

Both `theodi.org` URLs HTTP 200. No JSON-LD `datePublished` / `article:published_time` /
`dateModified` / DC.Date in static HTML — but each page carries exactly **one** first-party
`<time datetime>` element with matching human-readable text:
- "New research questions the trustworthiness of AI chatbots for government information"
  → **2026-02-05** (`<time datetime="2026-02-05">Thu Feb 5, 2026</time>`)
- "ODI and SAP announce partnership… trustworthy AI-ready data infrastructure"
  → **2026-03-24** (`<time datetime="2026-03-24">Tue Mar 24, 2026</time>`)
Single `<time>` per page = the article publish stamp, no listing ambiguity; datetime attr == visible
text. First-party bar met.

synth-feed (dry-run then write): 237 existing preserved, **2 new `press` events added (both odi)**,
0 conflicts. **feed.json 237 → 239.** odi.json news[] nulls 2→0. Both JSON re-validated; both
events confirmed present.

## Carried forward (updated)
- Non-gated null-date backlog now genuinely exhausted across publications[] AND news[].
- Gated tier still dominant: cetas (1), public-io (1), alan-turing (3), ico-ai (2),
  ~43 firms/consultancies/regulators — all need a browser-fetch path.
- Ada Lovelace: 1 deliberate null (Nuffield-co-published, no first-party date).

## 13:38 — news[] sweep across remaining gated firms (1 filled → dashboard-visible)

Applied the ODI-style lesson (news[] is ingested as press events, prior publications-only sweeps
missed it) to the remaining null-dated items, which are now ALL in news[]:
- **demos** "One in three UK adults report seeing political deepfakes…" → **2026-06-01**
  (JSON-LD datePublished 2026-06-01T14:23:58Z == article:published_time; HTTP 200). Excluded the
  dateModified 2026-06-17 (CMS quirk) and the body <time>2026-05-30 (referenced-data date, not the
  publish stamp) — same date-provenance discipline as prior demos/ada-lovelace refines.
- **ofcom-ai** "Online Safety Act… phased enforcement" → honest no-op: url is the /online-safety
  section landing page (301 → no per-article first-party date).
- **i-dot-ai** "i.AI merges into the new GDS" → honest no-op: target is the gov.uk *organisation*
  page. Only govuk:first-published-at (2023-11-20, org-page creation) and govuk:updated-at
  (2026-06-03, page-refresh stamp) available — neither is a trustworthy publish date for the 2026
  merger announcement. Left null rather than pad with a page-refresh date.

synth-feed (dry-run→write): 239 preserved, **1 new press event (demos)**, 0 conflicts.
**feed.json 239 → 240.** demos.json nulls 1→0. JSON re-validated; event confirmed present.

## Carried forward (updated)
- Remaining 11 nulls now all genuinely gated: alan-turing (3, Cloudflare), ico-ai (2, DC.Date-only),
  cetas (1), public-io (1), ofcom-ai (1, section page), i-dot-ai (1, org page),
  ada-lovelace (1 deliberate, Nuffield co-pub), demos done.
- Gated tier needs a real browser-fetch path that surfaces per-document first-party publish dates;
  curl/static-HTML lever is now fully exhausted.

## 01:46 (Jul 2) — re-scanned null-date backlog; 1 more filled (public-io blog-post)

Prior "non-gated nulls exhausted" note held for the firms it enumerated, but a fresh
grep surfaced items not on that gated list: **frontier-economics (1)**,
**slaughter-and-may-ai (1)**, **public-io (2, up from 1)**. Ran them through the
curl/static-HTML date lever (JSON-LD, og/DC.Date, `<time>`, visible meta text):

- **public-io** "Phase One Complete: … Thames Freeport" (blog-post) → **2026-06-23**.
  First-party stamp in the post's own `blog-meta-wrapper` (`<p class="p-14px">23 June 2026`),
  sitting inside the main post body right after the head image — not a listing/related block.
  HTTP 200. Filled.
- **public-io** "PUBLIC Bolsters Executive Leadership Team" (press-post) → honest no-op.
  press-post template has no `blog-meta-wrapper`; the only dates on the page (2021–2023) all
  belong to *other* listed posts. No first-party publish stamp → left null.
- **frontier-economics** "AI adoption: healthcare or health scare?" → honest no-op.
  Article `<h1>` present but carries no own date; all `icon-clock js-topic-metatext` dates
  (Nov/Aug/Mar/Jan 2025) belong to related-article listing tiles, not the target. No JSON-LD /
  og / DC.Date / `<time>`. Left null (gated to browser-fetch path).
- **slaughter-and-may-ai** "Regulating AI" → honest no-op. No JSON-LD/og/DC.Date/`<time>`
  and no visible date text at all (imported-content page). Gated.

synth-feed (dry-run→write): 293 preserved, **1 new press event (public-io)**, 0 conflicts.
**feed.json 293 → 294.** public-io nulls 2→1. JSON re-validated; event confirmed present.

## Carried forward (updated)
- Genuinely gated nulls: alan-turing (3, Cloudflare), ico-ai (2, DC.Date-only), cetas (1),
  public-io (1, press-post no own-date), ofcom-ai (1, section page), i-dot-ai (1, org page),
  frontier-economics (1, related-tiles only), slaughter-and-may-ai (1, imported content),
  ada-lovelace (1 deliberate, Nuffield co-pub).
- Curl/static-HTML lever confirmed exhausted again — remaining nulls all need a real
  browser-fetch path surfacing per-document first-party publish dates.
