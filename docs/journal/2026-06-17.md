# Journal — 2026-06-17

Append-only record of build decisions and progress. Newest at top.

---

## 16:48 UTC — Design system absorbed from europe2031.ai

**Did:**
- Read user reference: https://europe2031.ai/. Extracted palette + type personality from the HTML directly.
- Wrote `docs/DESIGN.md` codifying the system: warm off-white surfaces, ink + slate text, single muted-blue accent, warm clay-red for signal, hairline rules only.
- Typography: **Fraunces** (serif, human words), **Inter** (small UI), **Geist Mono** (data + eyebrows). Two type personalities is what does most of the design work.
- Wired tokens into Tailwind v4 `@theme` and rewrote `globals.css`.
- Updated `layout.tsx` to load the three font families with CSS variables.
- Rewrote `/` to use the new system: eyebrow + serif headline + restrained meta strip. No shadows, no gradients, no SaaS-y rounding.
- Build is clean.

**Why:** User asked for the aesthetic to feel like europe2031.ai — editorial, thoughtful, grown-up. That's a much better fit for a policy-thinking tool than a stock dashboard look. It also reads as commissioned-quality work, which serves the surprise discipline.

**Decisions:**
- Picked Fraunces over alternatives (Newsreader, Söhne) because it's free, variable, and has optical sizing built in — fits the editorial brief without licensing friction.
- No dark mode in v0. The warm off-white is the whole aesthetic.
- Cat presence kept to two locations only: the wordmark and the future "lobbycat says" callout. Discipline.

**Next:** Drizzle install + schema + first migration to Neon.

---

## 16:32 UTC — Execution journal started

**Did:**
- Created the execution-journal skill (`~/.openclaw/plugin-skills/execution-journal/`).
- Started this file. Backfilled the day's entries below from memory + commit history.

**Why:** Project will span multiple sessions and possibly an agent compaction. A journal in the repo means a returning human or fresh session can see decisions and progress without reading transcript.

**Next:** Drizzle schema + migration onto Neon.

---

## 16:27 UTC — Personalised fit notes scoped (v0.6)

**Did:**
- Added `v0.6 — Personalised fit notes` section to `docs/IMPLEMENTATION.md` (commit `a353ee4` and follow-up).
- Pulled grounding profile from `nwspk/politech-awards-2026` PR #73 (soul mirror, constitutional analysis, verified evidence).
- Wrote `research/profile-user.md` distilling background, weights, and career-decision context.

**Why:** User asked for a per-company modal that says, specifically and warmly, why this role could be interesting. Grounding it in the existing soul-mirror analysis gives us a verified evidence base — fit notes will cite real publications/lobbying/team-gap moves on the company side, real background on the user side.

**Decisions:**
- Decided to commit `research/profile-user.md` to the repo (vs. keep private) because (a) the soul mirror it's built from is already public on `nwspk/politech-awards-2026`, (b) transparency about how fit notes are grounded beats hidden magic.
- Decided fit notes are honest, not flattering — if a company is a stretch on a key axis, say so.
- Feature flag default OFF until user profile + framing are confirmed.

**Next:** Continue with v0.1 build (schema → grid → detail). v0.6 lands after the subjective layer (v0.2) is real.

---

## 16:24 UTC — Repo description sanitised

**Did:**
- Edited repo description via `gh repo edit` to remove gift-coded language.
- New description: "🐱 A quiet dashboard for loud decisions."

**Why:** Initial description contained language unsuitable for the eventual repo audience. User caught it.

**Decisions:**
- Surprise-discipline rule: anything that lands in a public-readable field (repo description, commits, PR titles, README, deployed URLs) must read as if commissioned, not gifted. Sentiment lives only in private workspace files.

**Next:** Continue build; treat journal entries as audience-readable too.

---

## 16:23 UTC — Neon Postgres provisioned

**Did:**
- Created project `lobbycat` (id `calm-night-11385649`) in region `aws-eu-west-2`.
- Saved connection string to `.env.local` (gitignored).

**Why:** London region for low latency to UK-based user. Default Neon free tier is sufficient for v0.

**Decisions:**
- Decided Postgres on Neon over local SQLite — user provided an API key (org-scoped, works for project creation).
- Decided `eu-west-2` over US regions.

**Next:** Install Drizzle, write schema, generate first migration, apply to Neon.

---

## 16:13 UTC — Scaffold pushed

**Did:**
- Created private repo `sugaroverflow/lobbycat`.
- Scaffolded Next.js 16 (App Router) + TypeScript + Tailwind v4 + src dir + ESLint via `create-next-app`.
- Replaced default home page with a branded placeholder ("A quiet dashboard for loud decisions. v0 · napping").
- Wrote initial `README.md` describing planned stack.
- Initial commit `283412d`, pushed to `main`.
- Followed up with `docs/IMPLEMENTATION.md` (commit `a353ee4`).

**Why:** Standard surface to build on. Next.js 16 + Tailwind v4 are recent but stable; App Router gives us server actions which reduce API boilerplate.

**Decisions:**
- Decided Next.js + Tailwind + shadcn over alternatives because the project mixes UI (grid, detail, comparison), light backend (cron, DB writes), and an agent endpoint — Next.js handles all three without extra infrastructure.
- Decided Drizzle over Prisma because the schema is small and we want zero magic.

**Next:** Provision Neon, install Drizzle, write schema.
