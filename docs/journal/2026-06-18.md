## 18:50 UTC — Step 6b: richer hover-card + sticky pin

**Did:** Upgraded the map hover-card from the 6a v1 (name + tier eyebrow + HQ + two scores) to a properly informative card and added a click-to-pin interaction so it stops disappearing the second the cursor moves off a dot. `MapCompany` gains a `fitNotePreview: string | null` field; `getMapData()` now fetches all `fitNotes` (`desc(createdAt)`) and, via a small `firstBullet()` parser that mirrors `fit-note-panel.tsx` (skips `caveat:` lines, strips `- `/`*`/`•` prefixes), surfaces the first bullet of the latest note per company. `MapView` keeps a separate `pinned` state alongside `hovered`; the active card is `pinned ➔ hovered` so a click locks the card open even as the cursor drifts. Clicking the same pinned dot again, or the new × button in the card, or any blank area inside the plot rect, unpins. Pinned dots get a translucent tier-coloured halo ring at `r=11`; the card itself swaps from `pointer-events-none border-rule` to `pointer-events-auto border-moss/40 ring-1 ring-moss/20` so the ×, the name link, and a new `Open profile →` row are all clickable. Card body now renders up to 4 `TagChip`s under the header, the two axis scores in a tighter `dl`, and (when present) a hairline-separated 3-line `line-clamp-3` fit-note preview with a `From the fit-note` eyebrow. `tsc --noEmit` and `next build` clean (9 routes, `/` still static). Marked 6b ✅ in `REFACTOR-v0.3.md`. Commit `86f6b98`, pushed.

**Why this shape:** Splitting `pinned` from `hovered` (rather than overloading one state) is what lets the card stay useful: hover is for cheap discoverability, pin is for actual reading. Falling back `pinned ➔ hovered` for the active id means the v1 hover behaviour is preserved exactly when nothing is pinned, so no one loses the old interaction. Surfacing only the first bullet of the latest fit-note (instead of the whole body, or instead of summarising) is the right level for a map card: the bullets prompt already enforces ≆22 words per line, so a single bullet is roughly card-shaped, and the `line-clamp-3` is a belt-and-braces guard against a pre-3b paragraph-shaped legacy note blowing out the layout. Doing the parsing in queries (not in the client) keeps `MapCompany` a plain serialisable shape and avoids shipping `firstBullet` into the JS bundle. The `border-moss/40 ring-1 ring-moss/20` treatment for the pinned state is the smallest visual delta that reads as "this is held open" without introducing a modal or a new accent token.

**Next (6c):** Filter chips (tier, HQ region, tag) above the plot that reduce the plotted set in place, plus an axis-swap button between the two axis pickers. Filters should also reduce the legend counts and the `unscored` footer so the numbers stay honest. Probably ~25–30m, then step 6 closes and we move to step 7 (Live tracker table) — still inside the v0.3 plan.

---

## 18:30 UTC — Step 6a: The Map scaffold (route split, axis pickers, SVG scatter v1)

**Did:** Started step 6 by splitting the home route. Old tier-list home moved verbatim to `src/app/companies/page.tsx` (renamed `HomePage` → `CompaniesPage`, eyebrow + h1 + intro re-copy to read as the long view rather than the landing) and the new `src/app/page.tsx` becomes the Map: queries a fresh `getMapData()` helper (scale-kind frames + companies with a `scores: Record<frameId, score>` map + tag list, all in parallel) and hands it to a new `MapView` client component. `MapView` renders two `<select>` axis pickers above an SVG scatter (720×480 viewBox, w-full responsive). X scale derived from `xFrame.scale`, Y likewise; grid lines at every integer with the 1/N edges drawn solid and the interior ticks dashed. Points are tier-coloured (`--color-terracotta`/`--color-ochre`/`--color-mushroom`) filled circles, opacity 0.78 → 0.95 on hover with an ink-tinted stroke ring; duplicate `(x,y)` collisions are spread by a golden-angle deterministic spiral so a 4-on-4 stack doesn't read as one dot. Hover surfaces a card pinned to the top-right of the plot with company name, tier eyebrow, HQ mono-line, and both axis scores; the company name links through to `/companies/[slug]` (card has `pointer-events-none` with the link re-enabled so the pointer doesn't fight the SVG hover). Axis labels use `lowLabel`/`highLabel` at the ends of each axis with the frame name centred below/rotated-left, all in the existing mono token. Below the SVG: a tier legend with per-tier plotted counts, and a `<details>` panel listing companies missing a score on the current pair so the gaps are honest rather than hidden. Site-shell nav gains a `Companies` entry next to `Map` so the tier list stays one click away. `tsc --noEmit` clean. `npx next build` clean (9 routes; new `/companies` static, new `/` still static since `getMapData` reads from the request-scoped db client). Updated `REFACTOR-v0.3.md` to mark step 6 in-progress with 6a notes. Committing + pushing.

**Why this shape:** Hand-rolling the scatter as SVG (rather than reaching for `recharts` or `visx`) keeps the bundle thin and means the visual restraint of the rest of the app — cream surface, hairline grid, mono ticks — carries straight into the chart without fighting library defaults. The golden-angle jitter for duplicate coordinates is the small correctness move that makes the v1 honest: on 30 companies and a 1–5 axis, collisions are guaranteed, and a single dot "hiding" three companies would be a worse failure than a tiny visible cluster. Pinning the hover-card to a fixed corner (instead of following the cursor) sidesteps the SVG-to-DOM coordinate transform entirely for this 15-minute slice; step 6b can replace it with a cursor-anchored variant if it earns its complexity. Keeping `getMapData` separate from `getCompaniesWithTags` (rather than extending the latter) means `/companies` keeps its lean open-roles query and the map doesn't pay for joins it won't render. Moving the tier list verbatim to `/companies` instead of rewriting it preserves the v0.2 surface as a roll-back lane while the map iterates.

**Next (step 6b):** Richer hover-card — tag chips, fit-note one-liner, and a click-to-pin so the card sticks while you scroll the legend. Probably 15–20m. Then 6c: filter chips (tier, HQ, tag) that fade non-matching dots, and a swap-axes button. After 6c we move to step 7 (live tracker table).

---

## 18:21 UTC — Step 5c: cat-suggested frames (step 5 ✅, checkpoint deploy)

**Did:** Closed out step 5 with a structured-JSON cat-suggest affordance. New `suggestFrames` server action in `src/app/actions.ts` reads the user profile, all existing frames, all tag labels, and the deduped focus-area set across companies, and sends them to Claude (sonnet-4.5, 1200-token cap) as grounding alongside a STRICT-JSON-only system rule. The user prompt asks for 2–3 NEW frames that fill a real gap in the existing set, biased toward question-kind but allowing one scale-kind frame if a numeric axis sharpens the decision, and pins the return shape to a fixed `{ frames: [...] }` schema with per-kind required fields. Response parsing is forgiving (strips stray ```json fences) but validation is strict: each candidate is rejected unless `name` + `kind` are present, question-kind has a non-empty `prompt`, and scale-kind has both `lowLabel` + `highLabel` (scale value gets clamped 2–10). Exported a `SuggestedFrame` type so the editor can render the candidates without re-deriving shape. Added a `CatSuggestions` component at the top of `frames-editor.tsx` — sage-soft panel with a `CatMark` + headline, a `✨ suggest frames` button that fires the action in a transition, and a list of returned candidates each with name + kind eyebrow, italicised prompt (for question) or low→high axis line (for scale), description, mushroom-italic "why" rationale, and a per-card `+ add` button that calls `createFrame` and flips to `✓ added` on success. Errors render terracotta inline; clicking `✨ ask again` re-rolls and clears the added-set. Updated `REFACTOR-v0.3.md` to mark step 5 ✅. `tsc --noEmit` clean, `npx next build` clean (8 routes, all green). Committing + pushing — this is the **checkpoint deploy** before the map and tracker work.

**Why this shape:** Returning a `SuggestedFrame[]` from the action (not writing to the DB inside `suggestFrames`) means the editor stays the only writer for frame rows, so the same `createFrame` path that handles manual adds also handles cat-suggested adds — one validation surface, one revalidate, one rollback story. Putting the JSON-schema instructions in the user prompt (rather than relying on Anthropic structured outputs / tool use) keeps this single-call and cheap; the strict per-kind validator on the server is the safety net that makes that choice safe, since a malformed candidate gets silently dropped instead of crashing the editor. Sage-soft background on the panel is the right visual signal: it reads as a "this is a tool, not a row" affordance — clearly distinct from the kind sections below — without breaking the editorial restraint of the rest of the page. The per-card "+ add" (rather than a "add all" footer) is the small UX honesty: Aadi is supposed to curate which gap-fills are real, not accept a batch.

**Next (step 6):** The Map. 2D scatter on the home route with axes selectable from any two scale-kind frames, hover-cards per company, click-through to `/companies/[slug]`. 75m time-box; ship a usable v1 even if it's not jaw-dropping. Step 5's CRUD + suggestions mean the user can already shape the axis vocabulary before the map lands, which is the dependency-order win the build plan was aiming for.

---

# Journal — 2026-06-18


## 18:08 UTC — Step 5b: frames CRUD UI at `/frames`

**Did:** Rebuilt `/frames` from a read-only list into a working CRUD surface. New client component `src/components/frames-editor.tsx` groups frames by `kind` (scale / tag / question), each group with its own short blurb explaining the kind, an inline-editable row per frame (edit toggles the form in place, delete confirms then cascades), and a per-group `+ add <kind> frame` affordance that opens a kind-specific form. The single `FrameForm` switches its body by kind: scale-kind shows low/high labels + 1–N numeric scale (clamped 2–10), question-kind shows a required prompt textarea, tag-kind shows just name + description. Three new server actions in `src/app/actions.ts` — `createFrame` (places new rows at `max(sortIndex) + 1`), `updateFrame`, and `deleteFrame` — all go through a shared `normalizeFrameInput` helper that trims/validates by kind and nulls out the fields the chosen kind doesn't use, so a tag-kind frame can't accidentally carry stale `highLabel`/`prompt` data after a kind switch. All three actions `revalidatePath` `/frames`, `/`, and `/companies/[slug]` so company detail pages with `FrameScorer` pick up renames/scale changes on next nav without a hard refresh. `npx tsc --noEmit` clean, `npx next build` clean (8 routes, all expected). Updated the v0.3 table to mark 5b ✅. Committing + pushing.

**Why this shape:** Doing 5b as a pure client-component form on top of three thin server actions (rather than per-row server-rendered forms with hidden inputs) means the kind-switcher can show/hide the right fields without a round-trip, and the same `FrameForm` component services both create and edit — one source of truth for what a valid frame looks like in the UI. Centralising kind-specific validation in `normalizeFrameInput` server-side (not just in the form) is what makes that safe: the client can't sneak a question-kind frame in with a stale `highLabel`, because the server zeroes it. Putting `+ add` at the bottom of each kind group (rather than a single "new frame" button with a kind picker) is the small UX win that turns the page into a real editor — Aadi sees the three buckets, picks the one his new thought belongs in, and starts typing. The cascading-delete confirm is honest about what's happening: `frames` → `frame_scores`/`frame_answers` is FK-cascade in the schema, so the prompt tells the user that before they click.

**Next (5c):** Cat-suggested frames. A "✨ ask the cat to suggest frames" button that hits Anthropic with the user profile + the existing frame set + a short list of company tags, and proposes 2–3 new frames (mostly question-kind) that fill a gap in Aadi's current thinking, returning them in a structured-JSON shape that the editor can offer as one-click adds. Probably 20–25m. After 5c lands we hit the **checkpoint deploy**.

---

Append-only record of build decisions and progress. Newest at top.

## 17:46 UTC — Step 5a: frames gain a `kind` + `frame_answers` table

**Did:** Extended `frames` in `src/db/schema.ts` with a `kind` text column (`'scale' | 'tag' | 'question'`, default `'scale'` so the 6 existing v0.2 rows backfill cleanly as scale-kind without a data migration) and a nullable `prompt` text column to carry the question text for question-kind frames; left `scale`/`highLabel`/`lowLabel` in place as scale-kind-only fields (kept non-null with defaults so existing inserts don't break). Added a `frame_answers` table (companyId + frameId composite PK, `answer` text, `updatedAt` tz-stamp, both FKs `onDelete: cascade`) for free-text answers to question-kind frames — mirrors the `frame_scores` shape exactly so the queries layer in 5b can treat answers and scores as parallel concepts. Wired `frameAnswersRelations` (belongs-to company + frame) and added `answers: many(...)` to `framesRelations` and `frameAnswers: many(...)` to `companiesRelations`. Added a btree `frames_kind_idx` on `frames.kind` so future filters at `/frames` and the map's axis-picker (step 6) can split by kind without a seq scan. Exported `FrameAnswer` / `NewFrameAnswer` inferred types. Ran `drizzle-kit generate` → `drizzle/0002_serious_triton.sql`, applied it against Neon via a one-shot `postgres`-driver script (same split-on-`statement-breakpoint` pattern as 3a; script discarded after run since the migration files are the durable artifact). `tsc --noEmit` clean.

**Why this shape:** Making `kind` default to `'scale'` means zero data migration on the existing 6 frame rows — they stay valid scale-kind frames and the new column is invisible to anything that doesn't care. Keeping `frame_answers` as its own table (rather than a nullable `answer` column on `frame_scores`) means scale and question frames never share a row, and a future user can in principle convert a frame's kind without colliding values; it also keeps the queries clean (`leftJoin frameAnswers` vs `leftJoin frameScores` based on `frames.kind`). The `prompt` column being on `frames` itself (not a side table) is the right call because every question-kind frame has exactly one prompt and storing it inline keeps the CRUD UI in 5b a single form. Tag-kind frames don't need extra storage — they reuse `companyTags`, just rendered through the frames lens.

**Next (5b):** CRUD UI at `/frames` — list view grouped by kind, edit/add/delete forms, plus `createFrame` / `updateFrame` / `deleteFrame` server actions. Should be ~30m. Then 5c (cat suggestions) closes the step and we hit the **checkpoint deploy**.

---

## 17:32 UTC — Step 4: cat-3 wired into wordmark + fit-note panel

**Did:** New `src/components/wordmark.tsx` exports two tiny components — `Wordmark` (cat head + "lobbycat" lockup, default 24px) and `CatMark` (just the cat, sized via prop) — both wrapping `next/image` around the existing `public/cat/lobbycat.png` (the cat-3 candidate that was picked back at `ebc7494`). `site-shell.tsx` and `app/login/page.tsx` drop the 🐱 emoji in favour of `<Wordmark size={26} />` / `<Wordmark size={36} />`; the header lockup now sits on `items-baseline` with the cat self-centered against the serif baseline so the visual weight matches the type. `fit-note-panel.tsx` swaps the terracotta heart in the eyebrow for an 18px `CatMark`, and the no-fit-note empty state gets a 32px cat at 80% opacity tucked alongside the "the cat will think it over" copy so the dead state has presence. `grep -rn 🐱 src/` returns nothing — emoji is gone from the codebase. `tsc --noEmit` clean. Marked step 4 ✅ in `REFACTOR-v0.3.md`. Committed (`804324d`) and pushed.

**Why this shape:** Building `Wordmark`/`CatMark` as their own components (instead of inlining `<Image>` at each site) means step 9's onboarding overlay and the future map/tracker headers can reuse the lockup without re-deriving sizing + alt-text conventions, and if cat-3 ever needs to become cat-3b the swap is one file. Putting the cat in the fit-note eyebrow (rather than as a larger avatar to the left of the bullets) keeps the editorial restraint of the existing panel — the cat is *signing* the note, not narrating it — which leaves room for a bigger cat presence on the dashboard map in step 6 without competing with itself.

**Next (step 5):** Frames CRUD + frame kinds. Largest remaining step (~60m): adds a `kind` column to `frames` (`scale | tag | question`), a `frame_answers` table for question-kind frames, edit/add/delete UI at `/frames`, and a cat-suggest-frames affordance. This is the **checkpoint deploy** — after step 5 lands we have a roll-back-able point before the map and tracker work. Will split into 5a (schema + migration), 5b (CRUD UI), 5c (cat suggestions).

---

## 17:14 UTC — Step 3c: fit-note chat thread

**Did:** Built the conversational layer on top of fit-notes. New server action `sendFitNoteMessage({ companyId, content })` in `src/app/actions.ts` persists the user message immediately (so a downstream model error doesn't lose the turn), then calls Anthropic with a strict alternating user/assistant message list reconstructed from the `fit_note_messages` table — our `'cat'` role maps to `'assistant'`. Grounding (user profile, company facts, latest fit-note body) gets prepended to the FIRST user turn in the list rather than smuggled through `system`, so the Messages API alternation stays valid and grounding rides with the conversation. System prompt is the short conversational sibling of the bullets prompt: short replies (1–4 sentences), specific, no bullets unless asked. `getCompanyBySlug` in `src/lib/queries.ts` now fetches `fitNoteThread` (ordered `asc(createdAt)`) alongside the latest `fitNote`. `FitNotePanel` renders the thread under the bullets with a `you` / `lobbycat` eyebrow pattern (sage-soft left rule for user turns to read as a quote-like signal), and a textarea + send button (⌘/Ctrl+Enter to submit, 2000-char cap, optimistic clear-on-send with draft restoration if the action throws). Thread UI only mounts once a fit-note exists, so empty-state companies don't show a dangling chat box. `tsc --noEmit` clean. Marked step 3 ✅ in `REFACTOR-v0.3.md`. Committed (`05704e5`) and pushed.

**Why this shape:** Grounding-on-first-user-turn (rather than appended to `system`) means the cat's behaviour rules and the company facts stay separable — I can iterate the persona in `system` without invalidating cached prompt prefixes, and the facts live where the conversation lives so future thread-summarisation can pick them up uniformly. Persisting the user message before the API call (instead of in a single transaction with the reply) is the correct durability story for chat: a 5xx from Anthropic shouldn't make Aadi's question vanish from the thread. The thread-only-after-fit-note gate is the small piece that makes the UI honest: there's nothing for the cat to follow up *on* until it's said something first.

**Next (step 4):** Wire the chosen cat illustration (cat-3) into the wordmark, fit-note panel header, and empty states. ~15m, mostly an asset + a couple of component edits. After that, step 5 (Frames CRUD + frame kinds) is the big one and the checkpoint deploy.

---

## 17:02 UTC — Step 3b: fit-notes regenerate as bullets + caveat

**Did:** Rewrote the Anthropic prompt in `src/app/actions.ts::generateFitNote` to emit a strict format — 3 to 5 short bullets (one per line, `- ` prefix, max ~22 words each, specific not generic) and an optional final `caveat: ` line for honest weaknesses (UK-pigeonhole risk, established-team-vs-build-from-scratch, etc.). Dropped the old "4–6 sentences, single flowing paragraph" instruction along with the "end with a short cited rationale" sign-off from the system prompt, since the caveat line now carries that load. Storage stays the same: the raw text lands in `fitNotes.body`, no schema change. Rewrote `src/components/fit-note-panel.tsx` to parse the body with a small line-walker (`parseFitNote`) that pulls out bullets and any `caveat:` line, then renders the bullets as a `<ul>` with moss-dot markers and serif body type, and the caveat as a hairline-separated muted paragraph with a terracotta `caveat` eyebrow. Legacy paragraph-shaped fit-notes still render: any line that isn't a bullet or caveat falls through as its own bullet, so v0.2 rows look fine until they're regenerated. `tsc --noEmit` clean. Committed (`dfd8828`) and pushed.

**Why this shape:** Keeping the storage as plain text in `fitNotes.body` (rather than a structured `bullets` jsonb column) means 3b ships without a second migration and the chat-thread layer in 3c can sit cleanly on top of `fit_note_messages` without entangling itself with how individual notes are stored. Putting the caveat on a dedicated `caveat:` line — instead of as another bullet — makes the honesty signal visually distinct in the UI (terracotta eyebrow, hairline rule) so Aadi can scan for it without reading the whole note, which is the whole point of the bullets rewrite.

**Next (step 3c):** Chat UI for the fit-note thread. Render `fit_note_messages` under the note, add a textarea + send action that appends to the thread and gets a cat reply grounded in the company facts + the existing note. Probably ~25–30m.

---

## 16:50 UTC — Step 3a: `fit_note_messages` table + migration

**Did:** Added the `fit_note_messages` table to `src/db/schema.ts` (id, companyId FK with cascade delete, `role` text holding `'user' | 'cat'`, `content` text, `createdAt` tz-stamp) plus two btree indexes (`company_idx`, `created_idx`) so per-company thread fetches stay cheap and chronological ordering is index-backed. Wired `fitNoteMessagesRelations` (belongs-to company) and added `fitNoteMessages: many(...)` to `companiesRelations`. Exported `FitNoteMessage` / `NewFitNoteMessage` inferred types. Ran `drizzle-kit generate` — produced `drizzle/0001_loving_talon.sql` and bumped `_journal.json` to two entries. `drizzle-kit push` requires a TTY and we don't have one in this runner, so applied the migration directly against the Neon DB via a small `postgres`-driver script that splits the SQL on `--> statement-breakpoint` and runs each statement under `sql.unsafe`. `tsc --noEmit` clean. Marked step 3 as in-progress in `REFACTOR-v0.3.md` with 3a checked off.

**Why this shape:** Keeping the message table tiny (no `metadata`, no `parentId`, no soft-delete) means 3c can build the chat UI without renaming columns later, and 3b can write rationale bullets into the existing `fitNotes.body` field while the conversation rides on the new table — i.e. fit-note storage stays where it is, and the *thread* is the new layer on top. Role is `text` not an enum so we can add `'system'` later without a schema bump if grounding messages get split out.

**Next (step 3b):** Rewrite the fit-note generator to emit bullets (3–5 short bullets + an optional honesty caveat) instead of a paragraph. Update `fit-note-panel.tsx` rendering to a `<ul>` with serif bullets. Will probably touch `src/lib/queries.ts` and `src/app/actions.ts` where regenerate lives.

---

## 16:42 UTC — Step 2c: human-readable tag taxonomy + DB migrated

**Did:** Atomic slug → sentence rewrite across `src/db/seed-data.ts` (30 companies, 11 tag rows): `hiring-policy` → `Hiring policy lead`, `frontier-lab` → `Frontier lab`, `voice-AI` → `Voice / media AI`, `agentic` → `Agentic / coding AI`, `self-driving` → `Autonomous / mobility`, `open-weights` → `Open weights`, `UK-HQ`/`EU-HQ`/`US-HQ` → `UK`/`EU`/`US`, etc. Synced the `VARIANTS` keys in `tag-chip.tsx` in the same commit so colour mapping never drifts from the DB. Dropped the `uppercase` Tailwind class from the chip base style so the new sentence-case labels read as sentences, not as shouts. Added a prune step to `src/db/seed.ts`: any tag row whose label isn't in the current taxonomy gets deleted (with `company_tags` cascading via the FK), so re-running the seed against an already-populated DB no longer leaves v0.2 slug rows orphaned next to the new labels. Ran `npm run db:seed` against the live Neon DB — pruned the 11 v0.2 slug rows and re-linked all 30 companies to the new human labels. `tsc --noEmit` clean. Committed (`f51553c`) and pushed; live URL now reflects the new taxonomy.

**Why split this way:** Step 2c was the last sub-step of step 2 (palette + pills + taxonomy combined into one row in the plan). Keeping seed-data and tag-chip in lockstep in a single commit means there's no moment in git history where the DB labels and the colour map disagree. The seed-side prune is the important infrastructure piece — without it, the prod DB would accumulate orphan tag rows on every taxonomy revision, and that's exactly the kind of v0.2-style sloppiness this refactor is trying to leave behind.

**Next (step 3):** Fit-note rewrite — new `fit_note_messages` table, conversational chat UI, and regenerate fit-notes as bullets instead of paragraphs. Largest step in the plan (~40m), so will split into 3a (schema + migration), 3b (regeneration to bullets), 3c (chat UI). Marking step 2 ✅ in `REFACTOR-v0.3.md`.

---

## 16:31 UTC — Step 2b: bigger pills + per-slug colour map

**Did:** Rewrote `src/components/tag-chip.tsx` to drive colour from a slug-keyed `VARIANTS` map instead of relying on the DB `color` column. Policy-posture slugs go terracotta / ochre / mushroom; product-shape and lab slugs go sage / moss; geography pills (`UK-HQ`, `EU-HQ`, `US-HQ`) stay deliberately neutral per the refactor spec. Pills are now `rounded-full`, `text-[0.7rem]`, `py-1 px-2.5` — visibly more pill-shaped and legible against the cream surface. Unknown labels fall back to a neutral wash with the legacy DB `color` applied to the text, so step 2c (slug → human-readable label rewrite in `seed-data.ts`) can land without breaking anything. `tsc --noEmit` clean. Committed (`dcfd948`) and pushed.

**Why split this way:** The colour map is keyed on slugs that exist in the DB *today*, so the visual change ships now without needing a reseed. Step 2c will rename the slugs to human strings (`"Hiring policy lead"`, `"Frontier lab"`, …) and update the variant keys in the same commit — atomic, revertable, and doesn't leave the seed and the component out of sync at any point.

**Next (step 2c):** Slug → human-readable taxonomy rewrite in `seed-data.ts`, sync the `VARIANTS` keys in `tag-chip.tsx`, and add the product-shape pills (`International policy`, `Product / GTM`, etc.) once we decide whether they're derived or stored.

---

## 16:14 UTC — Step 2a: earthcore palette tokens

**Did:** Swapped `globals.css` `@theme` block from the v0.2 steel-blue-on-cream system to the earthcore palette spec'd in `REFACTOR-v0.3.md`. Background is now warm cream (`#F4F1E8`), surface is paper (`#FFFEF8`), and the primary accent shifts from steel blue `#406896` to moss `#3E5C3A`. New first-class tokens (`--color-moss`, `--color-sage`, `--color-sage-soft`, `--color-sage-dark`, `--color-ochre`, `--color-ochre-soft`, `--color-terracotta`, `--color-mushroom`) are now available for the tag-pill colour map in step 2b. Kept `--color-accent`, `--color-warm`, `--color-positive` as aliases pointing at moss/terracotta/moss so every existing `text-accent` / `bg-accent` / `text-warm` className in the codebase shifts to the new system without a sweep. Hairlines rebalanced (`--color-rule: #D8D2BF`) so they read against cream instead of off-white. `tsc --noEmit` clean. Committed (`0cd9a03`) and pushed.

**Why split:** Step 2 in the plan estimates 25m for palette + tag pills + new taxonomy combined, but each piece is independent and the palette swap alone is a single atomic visual change that's safe to ship. Doing it as 2a/2b/2c lets each commit be revertable and keeps me honest about the 15-minute cadence.

**Next (step 2b):** Bigger tag pills + per-tag colour mapping in `tag-chip.tsx`, using the new sage/ochre/terracotta/mushroom tokens.

---

## 15:58 UTC — Step 1: /compare submission fixed

**Did:**
- Replaced the inline `dangerouslySetInnerHTML` `<script>` hijack on `/compare` with a proper `'use client'` `CompareForm` component (`src/app/compare/compare-form.tsx`). It owns the checkbox state in a `Set<string>`, builds the `slugs=a,b,c` query string on submit, and routes via `useRouter().push`.
- Updated `src/app/compare/page.tsx` to render the new component and pass `initialSelected={slugs}` so deep-links still hydrate the checked state. `tsc --noEmit` is clean. Committed (`4637eea`) and pushed.
- Marked step 1 ✅ in `docs/REFACTOR-v0.3.md`.

**Why:** The old approach relied on `document.currentScript` walking siblings — fragile under Next.js 16 hydration and obviously broken in practice. A client component is the right answer and unblocks step 2 (palette/tags) without leaving compare in a broken state.

**Next:** Step 2 — earthcore palette + bigger tag pills + new human-readable tag taxonomy. ~25m chunk.

---
