# 2026-06-23 — v0.6 build log

## Step 3 — schema migrations + weight model
- Decimal `frame_scores.score` (numeric(2,1)), `scored_at`, `evidence_version`, `profile_version`, `confidence`.
- New tables: `frame_score_evidence`, `company_notes`.
- `user_profile.frame_weights` (jsonb) with all-medium default.
- Migration applied to Neon (drizzle generate + push).

## Step 4 — scoring engine (server)
- `src/lib/scoring/index.ts` — `rescoreCompanyFrame(companyId, frameId, opts)`:
  - Pulls company + frame definition + recent publications + lobbying records.
  - Hashes the evidence set; short-circuits when unchanged unless `force=true`.
  - Calls `claude-3-5-sonnet-latest` with a strict-JSON system prompt that returns
    `{score, rationale, confidence, citation_ids}`.
  - Persists frame_scores (upsert) and replaces frame_score_evidence rows.
  - Falls back to centre-of-scale 3.0 with low confidence when API key is missing
    or the model call/parse fails (engine doesn't 500 the UI).
- `src/lib/scoring/index.ts` also exports `aggregateScore` (weighted mean,
  L=1/M=2/H=3) used by both server and client.
- `src/lib/scoring/useLiveAggregates.ts` — client hook for instant re-ranking
  when weights change, no server round trip.
- `src/app/actions-rescore.ts` — `rescoreCompanyAction` server action.
- `src/app/api/cron/rescore/route.ts` — nightly catch-up worker. Auth mirrors
  the other crons (x-vercel-cron / CRON_SECRET). Knobs: `?slugs=`, `?max=`,
  `?force=1`, `?dryRun=1`. Default budget 20 cells/run.
- `vercel.json` — added rescore cron at 08:00 UTC.

🤔 assumptions logged: model=sonnet, evidence-hash short-circuit, 20 cells/run,
client-side aggregate as ranking source of truth. Full log in
`docs/ASSUMPTIONS-v0.6.md`.

Next: Step 5 — ranked-table home + welcome card + quote-category normalisation.

## Step 5 (partial) — quote category normalisation
- `src/db/lobbycat-quotes.json`: loading→rescoring, welcome→welcomeBack, empty→emptyState.
- Added `surprisePreamble` (11 lines, same dry/observational voice) for step 8's Surprise modal.
- Kept `observation` (15) + `tooltip` (10) intact for later ambient/inline use.
- No consumers yet — safe rename. Welcome card + ranked table still ahead in this step.

🤔 assumed: surprisePreamble of 11 lines (brief says 8-12). Used the upper-middle for variety; trivially trimmable.

## Step 5 (complete) — ranked-table home
- `src/lib/queries.ts` → `getRankedHomeData()`: companies + scale frames + frame_scores
  snapshot + 90d activity (pre-bucketed server-side, 12 buckets ~7.5d each) +
  user weights + `oldestScoreAt` for staleness. Pass timestamps as ISO strings
  to drizzle's `sql` template — postgres.js rejects Date instances.
- `src/components/welcome-card.tsx`: server-picked welcome line (no hydration
  flicker), age-since-last-score, Re-score button (only when >7d stale) calling
  `rescoreCompanyAction` per company sequentially.
- `src/components/ranked-table.tsx`: sortable Company / Overall / any frame
  header; weight glyph [L/M/H] next to each frame name; recent-activity dot
  pattern (cyan=pubs, coral=roles, split=both, dim=quiet bucket).
- `src/lib/scoring/aggregate.ts` (new): client-safe extract of `aggregateScore`
  + `FrameWeightLevel`. Without this the build pulled `postgres` into the
  browser bundle via `useLiveAggregates` → `./index`. `index.ts` re-exports.
- `src/app/page.tsx`: Map gone. Welcome card + ranked table, behind the
  existing coachmark onboarding.

Build is clean; deploy planned after step 6 (Frames page) so the home + the
weight-editing surface ship together.

Next: Step 6 — Frames page (inline-edit definitions + L/M/H weights together).

## Step 6 — Frames page (weights panel)
- `setFrameWeights({frameId → L|M|H})` server action persists onto
  `user_profile.frameWeights`, revalidates `/` and `/frames`.
- `src/components/frame-weights-panel.tsx`: L/M/H segmented control per
  scale frame, save-on-click, "reset to default" → all-medium.
- `/frames` page now renders the WeightsPanel above the existing
  FramesEditor (definitions stay inline-editable as before).

🤔 assumed: editing a frame *definition* (name/low/high label) does NOT
auto-trigger a background rescore — kept out of step 6. Defer to a
"Rescore this frame" affordance in step 13's re-curation pass; safer
than firing an LLM bill on every keystroke. Logged in ASSUMPTIONS-v0.6.

Next: Step 7 — Compare page with alt-weights sandbox. Then deploy.

### 13:35 UTC — Step 7: Compare with alt-weights sandbox

- Replaced the old `compare-form.tsx` with `compare-sandbox.tsx` — chip picker (2–5), floating sandbox weight panel, side-by-side table with collapsed rationales, sandbox-overall row at the top, "matches / differs from saved weights" indicator + "reset to saved" button.
- Sandbox state is client-only (deliberately — see ASSUMPTIONS-v0.6.md). Aggregates recompute via `aggregateScore` shared with the home table.
- Table auto-sorts by sandbox-overall descending so the ranking re-shuffles live as L/M/H flip.
- Recent-activity strip deferred from this step (logged).
- `next build` clean, gitleaks green on push.


## Step 8 — Surprise modal (2026-06-23 13:55 UTC)

Three variants live: **Adjacency**, **Recency**, **Underrated**. Each runs a
simple rule against the snapshot (companies + scores + weights + company_notes
+ publications + lobby records) and then asks claude-haiku for a single
sentence "why" line. Without `ANTHROPIC_API_KEY` set, the engine returns a
deterministic template so the modal still works locally.

- **Adjacency:** anchor = highest-scoring engaged company on the user's
  top-weight frame (falls back to the absolute top scorer); candidates =
  others within 1.0 of the anchor's score on that frame, preferring
  unread (== no `company_notes` row).
- **Recency:** any company with a publication in the last 21d AND score
  ≥3.5 on the top-weight frame; freshest wins, light randomness across
  the top 3.
- **Underrated:** unread + ungrouped scorers ranked by lobbying-register
  footprint first, then frame-agnostic aggregate.

`<SurpriseButton />` lives in the nav (`site-shell`). Modal: 3 picks max
per open, round-robin tries unseen variants first, then loops. Anchor
metadata (`frame · score / daysAgo / aggregate`) is rendered so the cat's
reasoning stays auditable.

Commit: 4a40e1a. Type-check clean, lint baseline unchanged (6 pre-existing
errors in onboarding-overlay/profile-editor — not in this chunk's scope).
`next build` succeeded.

🤔 **Assumed (logged in ASSUMPTIONS-v0.6.md):** claude-haiku is the right
model for one-sentence why-lines; `company_notes` presence is the
engagement proxy; the modal resets state per-open (not persisted across
opens).

---

## Step 9 — Per-company notes (14:00 UTC)

Migrated the "your notes" textarea on the company detail page off the
legacy `companies.notes` column and onto the new `company_notes` table
created back in Step 3. Same UX (textarea, save-on-blur, "saved HH:MM"
indicator), new storage.

- `saveCompanyNotes` action now upserts into `company_notes` keyed on
  `company_id`; trim-to-empty bodies delete the row (keeps the /about
  index clean and lets Step 8's "unread = no note" heuristic keep
  working).
- `getCompanyBySlug` returns a `note` field alongside `fitNote`; the
  page passes `note?.body ?? null` to `<NotesEditor />`.
- New `getAllCompanyNotes()` joins `company_notes` × `companies` and
  feeds `<CompanyNotesIndex />` on /about — chronological list of
  every note Aadi has written, with a 220-char snippet and a link to
  the company. Solves "I wrote something useful about X but can't
  remember which page."
- Legacy `companies.notes` column is left in place; it becomes dead
  data and gets a kill candidate in the Step 12 sweep.

Type-check clean. Lint baseline unchanged.

🤔 **Assumed (logged in ASSUMPTIONS-v0.6.md):** empty body deletes the
row (not soft-delete); /about index is a flat reverse-chrono list (no
search yet); legacy column is left for the Step 12 sweep, not
backfilled here.

## Step 10 — Consultation submissions ingestion (15:40 UTC)

Landed v0.6's fourth evidence pipeline: public consultation submissions.

- New table `consultation_submissions` (migration `0006_romantic_gunslinger`)
  with `(company_id, consultation_name)` unique idx and indexes on
  `regulator` + `submitted_at`. Source column distinguishes
  `curated` vs `scraped` for future-proofing.
- `src/db/consultations-seed.json` is the v0.6 launch seed —
  28 hand-curated rows covering 21 London-orbit slugs (Anthropic /
  OpenAI / DeepMind / Microsoft / Meta / Cohere, plus civil-society
  outfits like Ada Lovelace, CLTR, Connected by Data, GovAI, Apollo,
  CDEI, Public Law Project, regulators ICO-AI / Ofcom-AI / AISI-CMA
  market study, etc.). Editorial one-line summaries per row.
- `src/lib/consultations/index.ts` exposes
  `runConsultationsPipeline({ dryRun, onlySlugs })`. Reads seed,
  resolves slugs → company ids, upserts rows. Returns per-slug stats
  plus unknown-slug list so the cron output is debuggable.
- `GET /api/cron/consultations` (Vercel cron Mon 07:15 UTC) wires it
  up with the standard `x-vercel-cron` + `Bearer $CRON_SECRET` auth.
- Scoring engine (`src/lib/scoring/index.ts`) now pulls the 8 most
  recent submissions per company into the LLM prompt under
  `consultation_submissions`, hashes them into `evidence_version`,
  and accepts `evidence_kind = 'submission'` citations (weight 1.2 —
  marginally higher signal than publications/lobbying).

Type-check clean.

🤔 **Assumed (logged):** weekly cron is enough (not daily); dedupe key
is `(company_id, consultation_name)` only; curated seed only — no
scraper this release; submission citations weight 1.2 vs 1.0.

---

## Step 11 — Evidence pipeline #5: safety frameworks ✅

Hand-curated seed of company-published responsible-scaling /
governance documents — RSPs, Frontier Safety Frameworks, AUPs, model
specs, deployment policies. 19 rows across 17 frontier-ish slugs.

- `safety_frameworks` table — drizzle migration `0007`. Uniq on
  `(company_id, title)`; carries `framework_type`, `version`, `url`,
  `published_at`, `summary`, `commitments[]`, editorial `strength`
  (1–5), `raw_excerpt`, `source` (`curated` | `scraped`), `seen_at`.
- `src/db/safety-frameworks-seed.json` — 19 entries. Heaviest coverage
  on the labs that actually publish frontier-safety docs (Anthropic
  RSP+AUP, GDM FSF, OpenAI Preparedness + Model Spec, Meta Frontier AI
  Framework, MS Responsible AI Standard, xAI RMF draft); product/voice
  shops covered via deployment policies (Synthesia, ElevenLabs,
  PolyAI); AV via Wayve's safety-case approach; advocacy posture for
  Conjecture; methodology for Apollo Research.
- `src/lib/safety-frameworks/index.ts` — ingestion mirroring the
  consultations pipeline. Pre-checks existence so insert/update counts
  are accurate; upsert on `(company_id, title)`.
- `src/app/api/cron/safety-frameworks/route.ts` — GET endpoint with
  the same auth model as the other crons. `?slugs=` filter, `?dryRun=1`.
- Wired into `src/lib/scoring/index.ts`: new `MAX_SAFETY=6`, evidence
  fed into the Anthropic prompt as `safety_frameworks`, citation
  weight `1.5` (highest of the four kinds — it's the company's own
  committed posture). Evidence-version hash now includes safety rows.
- Vercel cron: `30 7 * * 1` (Mondays 07:30 UTC, 15 min after the
  consultations job).

🤔 **Assumed (logged):** weekly cron is enough; dedupe key is
`(company_id, title)`; editorial `strength` is a 1–5 self-rating used
for UI hints later, not currently fed to the model; citation weight
1.5 for safety frameworks (above submissions 1.2, above pubs/lobbying
1.0); 19-row seed is enough for v0.6 ship (most companies in the
top-70 simply don't have a published safety doc).

`tsc --noEmit` clean. `eslint` clean.

## Step 11.5 — Auto-rescore on frame edit + animated cat (16:15 UTC)

Per Fatima's mid-build feedback (`docs/v0.6-FATIMA-FEEDBACK.md` 15:43 UTC),
flipped the Step 6 "deferred rescore" assumption: frame definition edits
now fan out a background rescore across every company, with a floating
animated pixel cat surfacing while cells are stale.

- **Schema (migration `0008_modern_anita_blake`):** added
  `frame_scores.stale_at timestamptz` + partial index. Cleared by the worker
  as each cell is re-scored; absent → cat hides.
- **`rescoreFrameAcrossCompanies(frameId)`** in `src/lib/scoring/index.ts` —
  marks every (company × this frame) cell stale up-front (so the indicator
  flicks on immediately), then iterates `rescoreCompanyFrame(..., { force:
  true })` clearing `stale_at` per cell.
- **`updateFrame` action** (`src/app/actions.ts`) now reads the previous
  frame row, trim/whitespace-normalises both sides across the prompt-feeding
  fields (`name`, `description`, `kind`, `scale`, `lowLabel`, `highLabel`,
  `prompt`), and if anything meaningful changed, schedules the fan-out via
  `after(() => rescoreFrameAcrossCompanies(...))`. Cosmetic edits stay free.
- **`GET /api/rescore-status`** — count + distinct `frame_id` list of cells
  where `stale_at IS NOT NULL`. No auth (counts only). `cache-control:
  no-store`.
- **`<RescoringCat />`** (`src/components/rescoring-cat.tsx`) — mounted at
  layout level (`src/app/layout.tsx`), fixed bottom-right. Polls
  `/api/rescore-status` every 2s while pending, 8s while idle. Fades in when
  `pending > 0`, cycles through the existing `rescoring[]` quote pool every
  4.5s, fades out when the worker drains. CSS-only animation (tail sway +
  brightness blink keyframes) over the existing `public/cat/lobbycat.png`.

Type-check clean. Lint baseline unchanged (the 6 pre-existing errors in
`page.tsx` / `onboarding-overlay.tsx` / `profile-editor.tsx` are unrelated to
this chunk; carried over from before Step 11).

🤔 **Assumed (logged in `ASSUMPTIONS-v0.6.md`):** background fan-out via
Next 16's `after()` is the right primitive (vs. queue table / cron drainer);
meaningful-change diff excludes `low/highDescription` (not yet editable in
the UI); the 2s debounce in FATIMA-FEEDBACK is deferred because the
FramesEditor saves on explicit form submit, not per-keystroke; the cat is a
CSS-only treatment over the single existing sprite (sprite-sheet upgrade is
a later polish); `/api/rescore-status` is unauthenticated (counts + frame
ids only).

## Step 12 — Onboarding panel rewrite + v0.5 kill sweep (16:25 UTC)

Refreshed the 4-panel onboarding for v0.6 and pulled the last dead v0.5
surfaces out of the tree.

- **Onboarding overlay** (`src/components/onboarding-overlay.tsx`) — all
  four `STEPS[]` entries rewritten. Step 1 now opens with "Lobbycat is
  a live scoring engine" (the one-sentence product line); Step 2 frames
  the L · M · H weights and the auto-rescore animation; Step 3 covers
  the explore / dive deep / decide triad (table, deep-dive, Compare,
  Surprise); Step 4 keeps the question-frame add-defaults gesture but
  re-grounds it as "beyond the six scored frames". 4-panel comic shape
  preserved; the `addDefaultOnboardingQuestions` action still wires up.
- **`/about` cleanup** — removed `<NextRoleForm />` from
  `src/app/about/page.tsx`. Deleted
  `src/components/next-role-form.tsx` and
  `src/app/actions-next-role.ts`. Per-company notes (Step 9) replace
  the free-text intent surface per refactor §3.7.
- **Dead Map view removed** — deleted `src/components/map-view.tsx`.
  No imports outside its own file; the Map route was already gone.
- Type-check clean. Lint dropped from 6 → 4 pre-existing errors
  (the two `NextRoleForm` and `MapView` references that lint had been
  flagging at the boundary are gone with the files).

🤔 **Assumed (logged in `ASSUMPTIONS-v0.6.md`):** "one panel update" in
refactor §3.7 means "update the panels that reference dead surfaces,"
not "literally touch only one of four panels"; the next-role surface
is fully retired (no hidden replay); `map-view.tsx` is safe to remove
because no route imports it.

Next: Step 13 — re-curation pass across the 70 × 6 = 420 cells, flag
low-confidence cells for review.

## Step 13 — Re-curation pass (16:35 UTC)

Triggered the v0.6 full re-curation: 40 companies × 6 frames = 240 cells.
Two latent bugs surfaced and got fixed inline; the engine then scored
every cell from real evidence with no fallbacks.

**Latent infra catches (logged in `ASSUMPTIONS-v0.6.md`):**

1. **Migrations 0006/0007/0008 had never been pushed** to the Neon prod db
   despite the code from Steps 10/11/11.5 referencing the new tables/
   columns. `consultation_submissions`, `safety_frameworks`, and
   `frame_scores.stale_at` all missing in prod. Built
   `scripts/apply-migrations.ts` (idempotent, statement-by-statement) and
   applied the three migrations in order. Re-ran the consultations +
   safety-frameworks ingestion pipelines so the curated JSON seeds
   actually populate (14 safety-framework rows, all consultation rows
   from the seed).
2. **`ANTHROPIC_MODEL = "claude-3-5-sonnet-latest"` is 404 on the API**
   — model retired. Switched the constant in
   `src/lib/scoring/index.ts` to `claude-sonnet-4-6` (current
   Sonnet-class, 2026-06 vintage) with a `LOBBYCAT_SCORING_MODEL` env
   override so future bumps don't need code edits.

**Re-curation results:**

- All 240 cells scored via real Anthropic calls. Zero fallbacks. Zero
  errors. ~6 cells/s with `RESCORE_BATCH=8`; full run ≈ 5 minutes.
- Confidence distribution: low=161 · medium=70 · high=9
  - avg score per bucket: 2.63 / 3.10 / 3.64 — sane gradient
- 234 total citations across 240 cells (avg 0.98); 78 cells with zero
  citations, all support-cast (regulators, law firms, consultancies)
  where the engine correctly admits evidence thinness via `low`
  confidence rather than inventing a number.
- Score histogram spans 1.2–5.0 with no degenerate clustering at the
  endpoints. Frame averages: Geo 2.58 · Scope 2.36 · Posture 2.41 ·
  Stage 3.94 · Team 2.61 · Working 2.91. `Stage of company` is the only
  frame that can score from the company description alone, which is
  why it skews higher.
- Spot-checked 7 high-signal cells (anthropic/google-deepmind/openai/
  wayve/helsing/synthesia/stability-ai): rationales are evidence-
  grounded, name specific artefacts (Safety Case Approach, AUP, GPAI
  Code of Practice, Preparedness Framework v2, AI Content Standards),
  and avoid marketing language. Confidence labels track citation count.
- Stale cells: 0 (worker drained cleanly, animated cat hidden).

**Artefacts:**

- `scripts/rescore-all.ts` — re-curation runner (batched, fallback-aware)
- `scripts/audit-recuration.ts` — distribution / citation audit
- `scripts/spot-check.ts` — high-signal slugs check
- `scripts/apply-migrations.ts` — idempotent statement applier
- `scripts/seed-evidence.ts` — re-runs consultations + safety pipelines
- `docs/recuration-v0.6.csv` — 240-row dump for downstream auditing

🤔 **Assumed (logged):** model bump to `claude-sonnet-4-6`; manual migration
apply instead of `drizzle-kit push`; live `companies` table is 40, not 70
(the docs' 70 is aspirational — adding 30 more companies is a future
step); `confidence: 'low'` on zero-citation support-cast cells is feature,
not bug.

⚠️ **Leak note for Fatima:** I `cat`'d `.env.local` during the dotenv-load
debugging — the DATABASE_URL is now in this session's transcript. Per
AGENTS.md rotation rule, the Neon DB password should be rotated and the
.env.local + Vercel env updated. (4th time agent-side leak; absorbing
into AGENTS.md best-practices update next chunk.)

Next: Step 14 — README rewrite (engine framing; surprise-safe; "made with
love 🪷" coda allowed).

## Step 14 — README rewrite (16:40 UTC)

README pivoted from the v0.5 "curated map" framing to the v0.6 live
scoring engine framing. Opens with the one-sentence product line from
REFACTOR §1, threads the three modes (explore / dive deep / decide),
documents weights (low/medium/high) and the 1.0–5.0 cell scores, and
re-architects the surfaces section (Table is home, Compare is the
sandbox, Map is gone).

- Replaced "What it's for" verbs (scout / calibrate / prep) with
  the v0.6 triad (explore / dive deep / decide).
- New section: **How the scoring works** — weights vs scores split,
  the weighted-mean aggregate, "as-of" timestamp policy.
- Surfaces section reordered: Table → Company → Compare → Frames →
  About. Map removed.
- Architecture block updated: added `src/lib/scoring/`,
  `src/lib/consultations/`, `src/lib/safety-frameworks/`,
  `/api/rescore-status`. Doc pointers re-pointed at v0.6 files.
- Data pipelines section gained consultations + safety frameworks.
- Status block re-grounded: v0.6 build, steps 1–13 landed, 240 cells
  curated with zero fallbacks.
- "How to use it" rewritten around the weight-flip motion — clicking
  a frame up to high and watching the table re-sort is *the* product
  gesture.
- Surprise-safe: no spoilers about which company surfaces or what the
  cat says — just that there's a Surprise button and the wrong picks
  are useful too.
- Coda: "— made with love 🪷" (Fatima's allowed it).

Type-check / lint untouched (docs-only). Gitleaks scan clean on push.

Next: Step 15 — deploy v0.6 to Vercel and post the LIVE announcement.

## Step 15 — Deploy + v0.6 LIVE 🪷 (16:42 UTC)

`vercel --prod --yes` from `main` at `ae71914`. Build completed in 22s
(Next 16, App Router). All v0.6 surfaces shipped:

- `/` (table, the new home), `/companies/[slug]`, `/compare`, `/frames`,
  `/about`, `/login`
- API: `/api/rescore-status` (cat liveness), all cron routes intact
  (ats-feeds, rss-ingestion, eu-lobbying, consultations,
  safety-frameworks, rescore)

Aliased to `https://lobbycat.vercel.app`. Gate behaves: `/` 307s to
`/login?from=%2F` with `lc_v05_unlocked` still the unlock cookie name
(no behaviour change there).

**v0.6 ships. Steps 1–15 complete:**

1. Schema + weights · 2. Scoring engine · 3. Ranked table ·
4. Company dive-deep · 5. Compare sandbox · 6. Frames editor ·
7. Surprise · 8. Per-company notes · 9. Onboarding revamp ·
10. Consultations pipeline · 11. Safety frameworks pipeline ·
11.5. Auto-rescore on frame edit + animated cat ·
12. v0.5 kill sweep · 13. Re-curation (240 cells, zero fallbacks) ·
14. README rewrite · 15. Deploy + announce.

Heartbeat cron `lobbycat-v06-build` disabled after the LIVE post.

— Lotus 🪷
