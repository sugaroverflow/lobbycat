# 2026-06-23 — v0.6 build journal

## Step 3 · Schema migrations + weight model — DONE

Picked up the v0.6 build at step 3 per the locked plan in `docs/REFACTOR-v0.6.md §10`. Steps 1 (concept sign-off) and 2 (data PRs — 70 companies + draft quotes) already landed.

### What landed

**`src/db/schema.ts`** — additive changes:

- `userProfile.frameWeights jsonb` with L/M/H map default `{ "1"..."6": "medium" }`. Typed as `FrameWeightsMap = Record<string, "low"|"medium"|"high">`.
- `frameScores.score` widened from `integer` to `numeric(2,1)` — supports the 1.0–5.0 scale agreed in §2.2. PG cast happened cleanly in place.
- `frameScores` gained `confidence` (`'low'|'medium'|'high'` text), `scoredAt`, `evidenceVersion`, `profileVersion` — the freshness/staleness substrate from §2.4.
- New `frame_score_evidence` table — per (company × frame) citation links, polymorphic by `evidence_kind` with `evidence_id` integer ref. Indexed on `(company_id, frame_id)` and `evidence_kind`. §4.2.
- New `company_notes` table — replaces v0.4 free-text intent; unique on `company_id`. §4.2 & §3.5.

**`drizzle/0005_material_nova.sql`** — generated migration. Applied to Neon by hand via a small node script (drizzle-kit migrate hung on the spinner; the SQL itself ran fine). Confirmed via `information_schema.columns` and a sample read on `user_profile.frame_weights` returning the default all-medium map.

**Call-site fixes** for the integer→numeric switch (drizzle returns `numeric` as string at runtime):

- `src/lib/queries.ts` — wrap reads in `Number(...)` at three Map/Record boundaries.
- `src/app/actions.ts`, `src/app/actions-next-role.ts`, `src/db/seed.ts` — convert score to `.toFixed(1)` string at insert/upsert sites.
- Typecheck passes (`tsc --noEmit` clean).

### What didn't land (deliberately)

- Didn't drop legacy `user_profile.weights` or any v0.5 free-text-intent surface — step 12's job. Keeping the running deploy alive between chunks.
- Didn't backfill `frame_score_evidence` for existing scores — there is no upstream evidence-linking data to backfill from yet (scores are currently editorially hand-set). Step 4 will populate as it rescores.

### 🤔 Assumptions

Five logged to `docs/ASSUMPTIONS-v0.6.md`:

1. In-place `ALTER COLUMN score TYPE numeric(2,1)` over a parallel column.
2. Convert numeric ↔ JS at boundaries vs. casting at the drizzle layer.
3. `frame_weights` backfilled by `ADD COLUMN NOT NULL DEFAULT` rather than explicit `UPDATE`.
4. Keep v0.5 columns alive across the v0.6 build (kill in step 12).
5. `frame_score_evidence.evidence_id` is a bare integer; polymorphic FK enforced in app layer, not PG.

### Next

Step 4 — scoring engine server:

- `rescoreCompany(companyId, frameId?, opts?)` server action — builds prompt, calls Anthropic, stores score + rationale + evidence rows.
- `/api/cron/rescore` background catch-up worker.
- `useLiveAggregates(weights)` client hook — instant overall recompute on weight changes.
