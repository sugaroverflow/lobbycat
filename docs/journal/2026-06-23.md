# 2026-06-23 — v0.7 build log

(v0.6 build log archived in git history at commit `1804e9b` and earlier.)

## Step 1 — Concept sign-off ✅
Signed off by Fatima 20:04 UTC. REFACTOR-v0.7.md is the source of truth.
v0.7-build heartbeat cron queued.

## Step 2 — Schema migrations (20:10 UTC)

Added the wizard-state columns to `user_profile` per REFACTOR §6.1.

- `wizard_completed_at TIMESTAMPTZ` — nullable. Set on wizard step-6
  submit; absence means "show the wizard, not the dashboard."
- `current_role_one_liner TEXT` — wizard step 2 input.
- `exploring_text TEXT` — wizard step 2 input.
- `location_preferences JSONB DEFAULT '{}' NOT NULL` — typed in the
  schema as `{ uk?, eu?, us?, remoteOk?, notes? }`.
- `open_text_answers JSONB DEFAULT '[]' NOT NULL` — typed as
  `Array<{ question, answer, answeredAt }>`.

No new tables (per §6.2). Weight-label rename to Must/Should/Could is
UI-only (per §6.1 last paragraph) — DB still stores `low|medium|high`,
aggregate numerics unchanged. Logged in ASSUMPTIONS-v0.7.md §F.

- `drizzle/0009_stiff_lucky_pierre.sql` generated.
- Applied to Neon via `scripts/apply-migrations.ts` (the idempotent
  applier from v0.6 step 13; survives a re-run).
- `tsc --noEmit` clean.

🤔 **Assumed (logged in ASSUMPTIONS-v0.7.md A–F):** wizard column is
nullable + no default (existing user re-enters via wizard, which IS the
v0.7 main flow); `current_role_one_liner` is a *new* column rather than
reusing `headline` (preserves v0.5 column, no one-way data migration);
JSONB rather than separate columns for location prefs (it'll grow);
weight-label rename stays at the React layer only.

Next: Step 3 — Vaporwave token system (calm cousin first: globals.css
palette swap, Orbitron + Share Tech Mono, firstScoring[] + fitNoting[]
quote arrays).

## Glyphie PR #9 — reviewed + merged (20:30 UTC)

Glyphie's first daily PR landed at 20:10. Three company feeds (OpenAI
London, Google DeepMind, Anthropic London) + global feed.json (5 events)
+ glyphie-notes/2026-06-23.md.

Notable: Anthropic's RSS feed 404s, so coverage is degraded to
headline-scraping for now. Glyphie correctly flagged this for Techie
(infra domain). Per USER.md domain split, I don't fix it; I just note
it's a known gap until Techie patches the ingestion pipeline.

Merged via `gh pr merge 9 --squash`. The branch couldn't be deleted
because Glyphie's worktree still references it; she'll reset her own
worktree on her next run. Not my problem to mess with.

## Step 3 — Vaporwave token system (20:50 UTC)

Calm-cousin tokens land. Surface look should change wholesale on next
load (palette + fonts), but no component code changes.

- `src/styles/vaporwave.css` written. Same role-named tokens as v0.5
  Machine (`--bg-canvas`, `--fg-prose`, `--accent-action`,
  `--readout-cyan`, `--signal-coral`, `--rule`, etc.), but values now
  point at the vaporwave palette: void purple `#090014` canvas, deep
  purple `#1a103c` panels, chrome silver `#E0E0E0` prose, hot magenta
  `#FF00FF` primary action, electric cyan `#00FFFF` readout, sunset
  orange `#FF9900` signal, muted dark purple `#2D1B4E` borders.
- Added vaporwave-only tokens for theatre surfaces:
  `--vw-sunset-gradient`, `--vw-accent-bar`, `--vw-grid-line`,
  `--vw-scanline`, `--vw-glow-magenta`, `--vw-glow-cyan`. These get
  reached by component CSS in later steps (wizard hero, scoring takeover,
  surprise modal).
- `src/app/globals.css` switched its import from `machine.css` to
  `vaporwave.css`. machine.css left on disk — it's the dedicated Step 12
  sweep job.
- `src/app/layout.tsx` swapped `Inter` → `Orbitron` (weights 400 / 500
  / 700 / 900) and `JetBrains_Mono` → `Share_Tech_Mono`. Wired via
  `--font-sans-loaded` / `--font-mono-loaded` so vaporwave.css is the
  single point of swap.
- `src/db/lobbycat-quotes.json` gained `firstScoring[]` (14 lines) and
  `fitNoting[]` (12 lines). Voiced dry, observational, third-person.
  Meta version bumped to `v0.7-build-step3`.
- Radii tightened: `--radius-tight` 4→2px, `--radius-panel` 10→4px
  (vaporwave is geometric — near-square reads right).
- `next build` passes clean (no TS errors, all 9 static pages
  generated). Old Machine palette gone; Vaporwave palette is live
  everywhere existing components used role tokens.

🤔 **Assumed (logged in ASSUMPTIONS-v0.7.md A–F):** role names
preserved across the swap (rename happens at Step 12 if it happens);
`--signal-coral` repointed to sunset orange #FF9900 (the canonical
third vaporwave accent); next/font (not self-host) for both faces;
radii tightened; machine.css left in place for the dedicated sweep;
quote counts (14 + 12) sit above the 10–15 minimum the refactor doc
suggests.

Next: Step 4 — The wizard (/wizard route, 6 steps, autosave per step).

## Step 4 — The wizard (20:50 UTC)

`/wizard` route ships. Six steps, autosave per step:

1. **Welcome** — vaporwave-tinted hero (perspective grid + sunset gradient), copy beat only, no persistence.
2. **Profile** — name, current-role one-liner, exploring-text, location prefs (UK/EU/US/Remote toggles + notes). Debounced autosave to `user_profile`.
3. **Frames** — read-only summary of the six pre-seeded scale frames with inline rename on blur. "Edit on /frames" link out for deeper edits (description / new frames / kind changes).
4. **Weighing** — Must / Should / Could buttons per frame, maps the existing `frameWeights` `low|medium|high` schema 1:1 (Must = high, Should = medium, Could = low). Per-click autosave.
5. **Open-text thoughts** — three default prompts ("what's making this hard / what's a great outcome / what are you afraid you're optimising for"). Debounced autosave.
6. **The big moment** — kicks `completeWizard`: sets `wizardCompletedAt`, fans rescore across all companies via Next 15's `after()` so the user isn't blocked. Theatre window: 25-sec progress bar, cycling `firstScoring[]` quotes (rotates every 3.5s), grid + sunset backdrop. Auto-bounces home on complete.

Front door: `src/app/page.tsx` now redirects to `/wizard` when `wizardCompletedAt` is null. `/wizard` redirects back home when set, except in `?replay=1` mode (used by the future /about replay-onboarding button).

Files added:
- `src/app/actions-wizard.ts` — `saveWizardProfile`, `saveWizardWeights`, `saveWizardOpenText`, `completeWizard`, `resetWizard`, `renameWizardFrame`.
- `src/app/wizard/page.tsx` — server fetch (profile, scale frames, company count, firstScoring quotes).
- `src/components/wizard.tsx` — single client component; step-state local; sub-components per step; shared `StepCard` / `Field` / `SaveBadge` atoms; `useDebouncedSave` hook.

Assumptions logged in `docs/ASSUMPTIONS-v0.7.md` §G–L. Highlights:
- Existing users get the wizard (no grandfather). Replay via `?replay=1`.
- Step 6 is the functional skeleton — full pixel-cat sprite + scanlines + 3D rotating perspective land in step 5 of the build (next chunk).
- Rescore runs in `after()` background, independent of the 25-sec theatre window. The existing v0.6 animated-cat indicator covers any straggler cells.
- Step 3 of the wizard is rename-only — the full FramesEditor would blow the 5-7 min budget; link-out to /frames preserves it.

`next build` clean. ESLint clean on touched files. `tsc --noEmit` clean.

Next: step 5 of the build — full vaporwave-theatre takeover for wizard step 6 (pixel cat sprite, scanlines overlay, deeper perspective grid animation).

## Step 5 — wizard step-6 vaporwave-theatre takeover

Upgraded `Step6BigMoment` in `src/components/wizard.tsx` from the
functional skeleton to the full vaporwave-theatre moment.

Shipped:
- Full-screen `fixed inset-0` overlay (covers site shell, z-index =
  `--z-modal`); progress bar hidden on step 6 so the moment reads as a
  moment, not a wizard step.
- Sunset backdrop: deep purple → hot pink → orange gradient with a
  radial sun disc behind the cat.
- Animated perspective grid floor (magenta + cyan lines, 60px cells,
  scrolling on a 4s loop, perspective rotateX 62°).
- CRT scanlines overlay (multiply blend, ~55% opacity).
- Pixel cat (lobbycat.png), 180×180, image-rendering: pixelated,
  floating + tilt animation, double drop-shadow glow (magenta + cyan).
- Orbitron uppercase title with sunset-gradient text-clip and magenta
  drop-shadow.
- Italic cycling quotes from `firstScoring[]` (re-key on quote change
  so React re-renders the line).
- Sunset-gradient progress bar with magenta glow and live
  "X / Y companies scored" readout.
- `prefers-reduced-motion` disables cat-float + grid-scroll.

`next build` clean. TypeScript clean.

Logged assumption M in `docs/ASSUMPTIONS-v0.7.md` covering the overlay
positioning approach and the reuse of `/cat/lobbycat.png` over the
unsplit 5-pose sprite sheet.

Next: step 6 — dashboard cards (stacked-list with collapsed/expanded
states, top-3 visible, show-more reveals).

## 21:15 UTC — Step 6: dashboard cards (stacked-list)

The ranked table dies. The cards land.

Built `src/components/dashboard-cards.tsx` (one card per company,
vaporwave calm-cousin styling — cyan top / magenta side neon borders
over deep-purple panels, straight geometry, no scanlines on this
surface per §4.1).

Top-3 visible without "show more" (per §3.2):
1. Top strip — company name (Orbitron, hover→cyan), HQ chip, one-line
   blurb, hiring badge (HIRING ● magenta-glow / hiring · unknown), and
   weighted Overall readout on the right.
2. Six frame score bars in a 2-column grid — score/5 → bar width, with
   the `vw-accent-bar` magenta→cyan gradient fill on scored cells and a
   greyed bar on unscored. M/S/C glyph beside each frame name (Must /
   Should / Could mapping per §6 — UI relabel, schema unchanged).
3. "Latest:" strip — newest publication OR newest open role
   (whichever is more recent), with a relative-time stamp.

Show-more reveals:
- Recent publications (last 6mo, top 6, newest first) with type icon.
- Open roles (top 6, currently-open) with department/location chip.
- "Fit-note + notes →" pill linking to the company page (where the
  fit-note panel + notes editor live untouched from v0.6).
- "✦ Fit-note ready" indicator when a fit-note exists.
- "Leave a note" link to /companies/{slug}#notes.

Data:
- Extended `getRankedHomeData()` in `src/lib/queries.ts` to fetch
  (in the same Promise.all) the last-6mo publications, all currently-
  open roles, and the set of companyIds that have a fit-note. Bucketed
  per-company into recentPublications / openRoles / openRoleCount /
  isHiring / hasFitNote / latestEvent.
- `home.details` flows through to `DashboardCards` alongside the
  existing scores/frames/weights.
- Activity buckets / `home.activity` is kept on the return shape for
  now — the welcome-back card (step 8) may still want it for the diff
  copy; if not, sweep-deletion (step 12) takes it.

Sort:
- Default sort = overall desc, nulls last (alpha tie-break). Filters
  and sorting toolbar land in step 7.

Page wiring: swapped `RankedTable` for `DashboardCards` in
`src/app/page.tsx`. Kept `ranked-table.tsx` on disk for now — step 12
sweeps it (along with /compare and any Machine residue).

TypeScript clean. `next build` compiles clean too — the prerender hit a
Neon "remaining connection slots are reserved for SUPERUSER" error on
/about and /frames, which is an infra issue, not a code issue. Flagged
for Techie (USER.md domain split). Vercel deploy goes through the
runner, which has its own connection ceiling, so this doesn't block
the live site.

Next: step 7 — filters + sorting toolbar at the top of the dashboard.

🤔 assumed: hiring badge is binary-or-unknown (true when openRoles > 0,
   null otherwise). We don't have an ATS-attached "NOT HIRING"
   signal for most companies yet, so "not hiring" only shows for
   companies that actively report zero open roles via their ATS
   source — which is none of them today. See ASSUMPTIONS-v0.7.md §N.

## 21:25 UTC — Step 7: filters + sorting toolbar

Landed sticky toolbar above dashboard cards.

- Sort options: Overall ↓ (default), Recent activity, Alphabetical, per-frame ↓ (one entry per scale frame)
- HQ select (derived from companies, "All" default)
- Tier 1/2/3 chip-toggles (multi-select)
- Filter chips: Hiring (isHiring===true), Open role (openRoleCount>0), Recent pub (recentPublications.length>0), Fit-note (hasFitNote)
- N/total counter + Clear button when any filter active
- Empty state when filters match nothing, with clear-filters CTA

Widened `getRankedHomeData` company select to include `tier`, propagated through the `Company` type in `dashboard-cards.tsx`.

🤔 assumed: "tier" in the spec maps to `companies.tier` (1/2/3); chosen multi-select (additive) rather than single-radio because Fatima often wants T1+T2 together. Logged in ASSUMPTIONS-v0.7.md.

Build ✓, gitleaks ✓, pushed. Next: step 8 — welcome-back card reading `research/feed.json`.

## 21:35 UTC — Step 8 welcome-back card

- New `user_profile.last_seen_at` column (migration 0010).
- New `recordHomeVisit()` query helper — bumps last_seen_at (5-min debounced), returns previous value.
- New `src/lib/welcome-back.ts` — reads `research/feed.json`, filters events to the diff window, ranks against user's high-weighted frames + company scores, returns top 3 named bullets + a residual count.
- `<WelcomeCard>` now renders a "New since you were last in…" panel under the quote when the feed is available. Empty window → muted "no new updates" line. Missing feed → panel hidden entirely (card degrades to bare quote, per spec).
- `next.config.ts` outputFileTracingIncludes added for `research/feed.json` so Vercel bundles it into the serverless function.
- Three 🤔 assumptions logged: diff window semantics, frame-relevance ranking, graceful degradation rules.

## Step 9 — Frames page redesign

- Added a three-card **Definitions** strip at top of `/frames` (scale / tag / question) so newcomers see what each frame kind is before scrolling into weights.
- Relabelled `FrameWeightsPanel` from L/M/H to **Must / Should / Could** (MoSCoW). Underlying enum (`low | medium | high`) and aggregate math unchanged — purely a copy swap so internals + persisted profile `frameWeights` are untouched.
- Renamed the suggestion CTA from "Ask the cat for frame ideas" → "Ask lobbycat for frame ideas" to match §10 spec wording. Haiku-backed `suggestFrames` action and one-click add affordance both already wired from v0.6, no behaviour change.
- "Add a frame" affordance: already present inside `FramesEditor` (per-kind sections). Leaving as is.
- `next build` clean.

## 22:10 UTC — Step 10: surprise modal vaporwave-theatre reskin

Lifted the existing surprise modal logic verbatim and reskinned the
shell to match §4.1 "moment surface" treatment.

- Backdrop now a void-purple → magenta vertical wash with the centred
  radial sun glow, blurred. Scanlines overlay on top.
- Perspective grid floor anchored at the bottom edge (40% height,
  rotateX 62°), scrolling slowly. Lighter opacity than the wizard
  takeover so it reads as a hint, not the whole stage.
- Headline "▸ Surprise" in Orbitron uppercase with the sunset gradient
  text fill + magenta drop-shadow (matches the wizard step-6 hero).
- Pick card: skewX(-2°) at rest → un-skews on hover with cyan glow
  pulse + slight translateY. Border cyan-top / magenta-side, neon
  shadow. Variant chip in sunset orange.
- Loading state shows the animated pixel cat (float + glow + tail
  wiggle via vw-cat-float keyframes) plus a quote-style line. Wires
  the cat into the surprise generation moment — step 13 will hook in
  fitNoting[] cycling for the fit-note loading state.
- Close affordance restyled to `[ esc ]` chip in cyan to match the
  theatre vocabulary.
- prefers-reduced-motion disables float, grid scroll, and skew.

`tsc --noEmit` clean. `next build` clean.

🤔 assumed: surprise modal stays a centred dialog rather than going
   fullscreen-takeover like the wizard step-6. §4.1 lists surprise
   under "moment" surfaces but doesn't mandate full-bleed; centred-
   theatre keeps multi-pick UX intact (3 picks per session, "show
   me another" affordance stays in view).

Next: step 11 — About + per-company notes index.

---

## 22:30 UTC — Step 11: About + per-company notes index ✅

Rewired /about to mirror the v0.7 wizard.

**ProfileEditor (rewritten):**
- 5 inline-edit sections, each with autosave via `updateProfile`:
  1. Name (displayName)
  2. Current role one-liner (currentRoleOneLiner)
  3. What you're exploring (exploringText) — multiline
  4. Location (locationPreferences) — UK / EU / US / Remote OK
     checkboxes + free notes textarea, with a chip-style display
     view
  5. Open-text thoughts (openTextAnswers) — repeatable
     question/answer rows the user can edit, add to, or remove
- Footer line points the user at /frames for the
  Must/Should/Could editor (per §3.5 spec separation).
- Dropped the v0.6 bio / concerns / free-text weights / sources
  sections from the surface (columns survive in DB for fit-note
  fallback; step 12 sweep will decide on final removal).

**updateProfile action:** extended to accept the four new fields
on top of the legacy patch shape. Trims, defaults locationPreferences
booleans, and re-stamps `answeredAt` on incoming answers when
missing.

**CompanyNotesIndex:** unchanged — reverse-chrono flat list with
220-char snippets and back-links to each company page. Already
v0.7-ready from a prior step.

**Replay onboarding link:** now clears *both* `onboardedAt` (legacy
coachmark) and `wizardCompletedAt` (v0.7 gate), drops the cookie
fallback, then routes the user to `/wizard` instead of `/`. Label
copy switched to "replay onboarding →" to match §3.5.

`tsc --noEmit` clean. `npx eslint` clean. `next build` clean — all
6 routes render.

🤔 assumed: About edits *only* the wizard-era fields; legacy v0.6
   bio/concerns/sources stay in the DB but are dropped from the
   editor surface (step 12 sweep decides on final removal). Frames
   + Must/Should/Could weights stay scoped to /frames.

Next: step 12 — sweep deletion (Machine CSS purge, v0.4/v0.5
residue check, compare-page full delete confirmation).

## 22:50 UTC — Step 12: sweep deletion

Killed:
- `src/app/compare/` (page + sandbox) — Map's evil twin from v0.4
- `src/components/coachmark-onboarding.tsx` — v0.5 driver.js tour
- `src/app/actions-mark-onboarded.ts` — server actions for the tour
- `driver.js` npm dep (`npm uninstall driver.js`)
- `src/components/expandable-company-row.tsx` + `tracker-table.tsx` — dead since DashboardCards landed in step 6
- `src/app/machine-test/` — v0.5 Machine swatch reference page

Patched:
- `site-shell.tsx`: removed `/compare` nav link + `data-coachmark` attr
- `app/page.tsx`: removed `CoachmarkOnboarding` mount + `onboardedAt` plumbing
- `replay-onboarding-link.tsx`: only calls `resetWizard()` now (cookie still cleared as a courtesy)
- `globals.css`: scrubbed Machine narrative from comments; tokens themselves were already Vaporwave

Build clean: `npx next build` green, type-check green. Routes down to: `/`, `/about`, `/companies/[slug]`, `/frames`, `/login`, `/wizard`, the cron endpoints.

🤔 left intact for now (logged to ASSUMPTIONS-v0.7.md):
- earthcore back-compat aliases in `globals.css` (cream-dark, ink, moss, sage, ochre, terracotta, …) — still ~21 callsites for `text-ink` alone, separate per-component sweep
- `user_profile` legacy columns (`onboardedAt`, headline, bio, concerns, free-text weights, sources) — fit-note grounding still falls back to them
- `TagChip` + tags on `/companies/[slug]` — read §9 as dashboard-only

Next: step 13 — loading animations on fit-note + surprise (animated cat with cycling quotes).

## 22:54 UTC — Step 13: loading animations (fit-note + surprise)

New `src/components/loading-cat.tsx` — shared calm-cousin
animated cat (sway + blink + soft cyan halo) with a rotating
quote line every 1.5s, accepting any quote pool. Reduced-motion
honored. `role="status"` + `aria-live="polite"` for a11y.

Wired in:
- **fit-note panel** — replaces the body during a `generate` /
  `regenerate` transition (`reading` / `re-reading` eyebrow);
  also replaces the bare "thinking…" line in the follow-up
  thread with a 32px cat + cycling `fitNoting[]` quotes.
- **surprise modal** — the existing theatre-style pixel cat
  stays (it shares the modal's scanlines + sunset gradient
  context); but the static "the cat is pawing through…" line
  is now a cycling `surprisePreamble[]` quote at 1.5s cadence
  with a fade-on-change animation.

`tsc --noEmit` clean. `npx eslint` clean. `next build` clean —
all routes still render.

🤔 assumed: surprise modal keeps its own inline theatre cat
   styling rather than swapping to the shared `LoadingCat`,
   because the shared component's `calm` palette would clash
   with the void-purple modal. Logged as a tone choice in
   ASSUMPTIONS-v0.7.md.

Next: step 14 — README rewrite (engine framing + v0.7 +
'made with love 🪷' coda).
