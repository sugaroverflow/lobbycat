# 2026-06-23 — v0.7 build log

(v0.6 build log archived in git history at commit `1804e9b` and earlier.)

## Step 1 — Concept sign-off ✅
Signed off by Fatima 20:04 UTC. REFACTOR-v0.7.md is the source of truth.
v0.7-build heartbeat cron queued.

## Step 2 — Schema migrations (20:10 UTC)

Added the wizard-state columns to `user_profile` per REFACTOR §6.1.

- `wizard_completed_at TIMESTAMPTZ` — nullable. Set on wizard step-6
  submit; absence means "show the wizard, not the dashboard."
- `current_role_one_liner TEXT` — wizard step 2 input.
- `exploring_text TEXT` — wizard step 2 input.
- `location_preferences JSONB DEFAULT '{}' NOT NULL` — typed in the
  schema as `{ uk?, eu?, us?, remoteOk?, notes? }`.
- `open_text_answers JSONB DEFAULT '[]' NOT NULL` — typed as
  `Array<{ question, answer, answeredAt }>`.

No new tables (per §6.2). Weight-label rename to Must/Should/Could is
UI-only (per §6.1 last paragraph) — DB still stores `low|medium|high`,
aggregate numerics unchanged. Logged in ASSUMPTIONS-v0.7.md §F.

- `drizzle/0009_stiff_lucky_pierre.sql` generated.
- Applied to Neon via `scripts/apply-migrations.ts` (the idempotent
  applier from v0.6 step 13; survives a re-run).
- `tsc --noEmit` clean.

🤔 **Assumed (logged in ASSUMPTIONS-v0.7.md A–F):** wizard column is
nullable + no default (existing user re-enters via wizard, which IS the
v0.7 main flow); `current_role_one_liner` is a *new* column rather than
reusing `headline` (preserves v0.5 column, no one-way data migration);
JSONB rather than separate columns for location prefs (it'll grow);
weight-label rename stays at the React layer only.

Next: Step 3 — Vaporwave token system (calm cousin first: globals.css
palette swap, Orbitron + Share Tech Mono, firstScoring[] + fitNoting[]
quote arrays).

## Glyphie PR #9 — reviewed + merged (20:30 UTC)

Glyphie's first daily PR landed at 20:10. Three company feeds (OpenAI
London, Google DeepMind, Anthropic London) + global feed.json (5 events)
+ glyphie-notes/2026-06-23.md.

Notable: Anthropic's RSS feed 404s, so coverage is degraded to
headline-scraping for now. Glyphie correctly flagged this for Techie
(infra domain). Per USER.md domain split, I don't fix it; I just note
it's a known gap until Techie patches the ingestion pipeline.

Merged via `gh pr merge 9 --squash`. The branch couldn't be deleted
because Glyphie's worktree still references it; she'll reset her own
worktree on her next run. Not my problem to mess with.

## Step 3 — Vaporwave token system (20:50 UTC)

Calm-cousin tokens land. Surface look should change wholesale on next
load (palette + fonts), but no component code changes.

- `src/styles/vaporwave.css` written. Same role-named tokens as v0.5
  Machine (`--bg-canvas`, `--fg-prose`, `--accent-action`,
  `--readout-cyan`, `--signal-coral`, `--rule`, etc.), but values now
  point at the vaporwave palette: void purple `#090014` canvas, deep
  purple `#1a103c` panels, chrome silver `#E0E0E0` prose, hot magenta
  `#FF00FF` primary action, electric cyan `#00FFFF` readout, sunset
  orange `#FF9900` signal, muted dark purple `#2D1B4E` borders.
- Added vaporwave-only tokens for theatre surfaces:
  `--vw-sunset-gradient`, `--vw-accent-bar`, `--vw-grid-line`,
  `--vw-scanline`, `--vw-glow-magenta`, `--vw-glow-cyan`. These get
  reached by component CSS in later steps (wizard hero, scoring takeover,
  surprise modal).
- `src/app/globals.css` switched its import from `machine.css` to
  `vaporwave.css`. machine.css left on disk — it's the dedicated Step 12
  sweep job.
- `src/app/layout.tsx` swapped `Inter` → `Orbitron` (weights 400 / 500
  / 700 / 900) and `JetBrains_Mono` → `Share_Tech_Mono`. Wired via
  `--font-sans-loaded` / `--font-mono-loaded` so vaporwave.css is the
  single point of swap.
- `src/db/lobbycat-quotes.json` gained `firstScoring[]` (14 lines) and
  `fitNoting[]` (12 lines). Voiced dry, observational, third-person.
  Meta version bumped to `v0.7-build-step3`.
- Radii tightened: `--radius-tight` 4→2px, `--radius-panel` 10→4px
  (vaporwave is geometric — near-square reads right).
- `next build` passes clean (no TS errors, all 9 static pages
  generated). Old Machine palette gone; Vaporwave palette is live
  everywhere existing components used role tokens.

🤔 **Assumed (logged in ASSUMPTIONS-v0.7.md A–F):** role names
preserved across the swap (rename happens at Step 12 if it happens);
`--signal-coral` repointed to sunset orange #FF9900 (the canonical
third vaporwave accent); next/font (not self-host) for both faces;
radii tightened; machine.css left in place for the dedicated sweep;
quote counts (14 + 12) sit above the 10–15 minimum the refactor doc
suggests.

Next: Step 4 — The wizard (/wizard route, 6 steps, autosave per step).
